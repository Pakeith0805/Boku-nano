# 芦原・倉林研究室　夏休み実習課題：Boku-nanoの開発

## 課題名

**日本語の指示から限定的なPython関数を生成するSmall Language Modelのフルスクラッチ学習**

本課題では、極めて限定されたPythonプログラミング領域を対象として、データ生成、トークナイザ作成、Transformerモデル構築、事前学習、実行評価までを一貫して行う。

対象: 学部4年生 / 期間: 6週間 / 計算資源: GeForce RTX 5090 × 1

必修モデルを「Boku-nano」、発展として作成しても良い中規模パラメータモデルを「Boku-mini」、そして、将来作成する約1Bパラメータのモデルを「Boku-1B」と呼ぶ。

## 課題の目的

本課題の主目的は、高いベンチマーク性能を達成することではない。したがって、到達精度そのものは成績評価の中心にしない。評価するのは、データ生成から学習・評価までのパイプライン全体を自分の手で組み、各段階で何が起きているかを説明できることである。

学生が次の一連の工程を自分で完成させ、「ランダムな重みから学習した言語モデルが、未学習の指示に対して実行可能なプログラムを生成する」ことを体験することが目的なので、ぜひ、自らの手で実装してみてほしい。

- 対象領域を機械的に定義する
- 正解プログラムとテストを自動生成する
- 教師モデルを使って日本語表現を増やす
- 合成データを検証・重複除去・分割する
- トークナイザをゼロから学習する
- Transformerをランダム初期値から学習する
- 生成コードを実際に実行して評価する
- 成功例だけでなく失敗例も分析する

## 用語の定義

本課題では、三種類のモデル・AIが登場する。役割が異なるため、混同しないよう区別して用いる。

| 用語           | 実体                             | 役割                                                   |
| -------------- | -------------------------------- | ------------------------------------------------------ |
| **学生モデル** | Boku-nano / Boku-mini            | 本課題でランダム初期値から学習させる対象               |
| **教師モデル** | `Qwen/Qwen3-4B-AWQ`              | オフラインで日本語の表現・解説文だけを生成する         |
| **開発支援AI** | Codex等のコーディング支援AI      | パイプラインの実装コードを書く作業を支援する           |

### 教師モデルと開発支援AIの境界

この二つの区別が本課題で最も誤解されやすい点である。判断基準は「**その出力が学習データそのものになるか、学習データを作る仕組みになるか**」である。

**開発支援AIの使用を許可する範囲**

- 参照インタプリタの実装
- コード生成器・日本語文生成器の実装
- 検証・重複除去・分割スクリプトの実装
- トークナイザ学習、モデル定義、学習ループ、評価スクリプトの実装

これらはいずれも「決定論的な生成器そのもの」を作る作業であり、AIの出力は生成器のソースコードであって、学習データではない。生成器がどのようなデータを出すかは、学生が書いた仕様とルールによって完全に決まる。

**禁止する使い方**

- 開発支援AIに個々の学習サンプル（指示文・正解コード）を直接書かせる
- 教師モデルに正解プログラムそのものを決めさせる
- インターネットから取得した既存コードを合成データへ混入させる

正解の意味構造、コード、テストはルールベースで作成し、教師モデルは自然言語（日本語）表現を増やす役割に限定する。

## 「フルスクラッチ学習」の定義

本課題では、次のすべてを満たすものだけをフルスクラッチ学習と呼ぶ。

1. 学生モデルの重みをランダムに初期化する
2. 既存モデルの重み、埋め込み、LoRA、Adapterを使用しない
3. トークナイザも本課題の訓練データだけから作成する
4. 教師モデルはオフラインでのデータ生成にだけ使用する
5. 学習時および評価時に教師モデルのlogitや内部状態を使用しない
6. 評価時には学生モデルだけでプログラムを生成する
7. テストデータの問題、言い換え、正解コードを訓練データへ混入させない

Transformerそのものは、PyTorchやTransformersの既存部品を利用してよい。ただし、学習済みモデルを読み込むのではなく、設定ファイルから新しいモデルを生成しなければならない。

### 将来構想

本課題は、研究室で構想している「Boku-1B」(合成データのみで学習した日本語・コーディング特化の1Bパラメータ級 Dense モデル)の予備実験にあたる。本課題を完了した時点で、Boku-1Bの設計に必要な実測データが手元に揃うことになる。

## 全体像

```
Phase 1  データ生成器 + 実行検証     2週
Phase 2  トークナイザ                1週
Phase 3  モデル実装                  1週
Phase 4  学習                        1週
Phase 5  評価                        1週
```

教師モデルによる日本語表現の生成と人間による承認作業は、Phase 1に含めて計画する。

## 対象とする問題

日本語の指示を入力として、次のシグネチャを持つPython関数を生成する。

```python
def solve(xs: list[int], k: int) -> list[int]:
    ...
```

固定するのは**関数名が`solve`であること**と**位置引数が`xs`、`k`の2個であること**の二点である。型注釈は、あってもなくてもよい（後述の構造的変換で、型注釈の有無を等価なコード表現として扱うため）。

入力条件は次のように限定する。

- `xs`は長さ0〜20の整数リスト
- 各要素は−100以上100以下の整数
- `k`は1以上10以下の整数
- 出力は整数リスト（二乗などの変換により、出力要素は−100〜100の範囲を超えてよい）
- 外部入力、ファイル、ネットワーク、乱数は使用しない
- 副作用を持たない純粋関数とする

## 対象とするプログラミング上の操作

以下の24種類の操作から1〜4個を組み合わせた問題だけを扱う。同一の操作を2回以上使用しない。

意味ASTやデータレコードの中では、次の**op名**を正式な識別子として使用する。本文中の例もすべてこの名前で記述している。

**抽出**（10種類）

| op名          | 内容                    |
| ------------- | ----------------------- |
| `even`        | 偶数だけを残す          |
| `odd`         | 奇数だけを残す          |
| `gt`          | `k`より大きい値を残す   |
| `ge`          | `k`以上の値を残す       |
| `lt`          | `k`より小さい値を残す   |
| `le`          | `k`以下の値を残す       |
| `multiple_of` | `k`の倍数だけを残す     |
| `positive`    | 正の要素を残す          |
| `negative`    | 負の要素を残す          |
| `zero`        | ゼロの要素を残す        |

**変換**（8種類）

| op名     | 内容                 |
| -------- | -------------------- |
| `add_k`  | 各要素に`k`を加える  |
| `sub_k`  | 各要素から`k`を引く  |
| `mul_k`  | 各要素に`k`を掛ける  |
| `double` | 各要素を2倍する      |
| `triple` | 各要素を3倍する      |
| `negate` | 符号を反転する       |
| `abs`    | 絶対値を取る         |
| `square` | 二乗する             |

**並べ替え**（3種類）

| op名      | 内容           |
| --------- | -------------- |
| `asc`     | 昇順に並べる   |
| `desc`    | 降順に並べる   |
| `reverse` | 逆順にする     |

**切り出し**（3種類）

| op名          | 内容                 |
| ------------- | -------------------- |
| `take_first`  | 先頭から`k`個        |
| `take_last`   | 末尾から`k`個        |
| `every_other` | 1個おきに取得する    |

## 問題例

入力指示：

> 整数リストxsから、まずk以上の要素だけを残し、次にそれぞれを2倍して、最後に昇順に並べるsolve関数を書いてください。

期待されるコードの一例：

```python
def solve(xs: list[int], k: int) -> list[int]:
    return sorted([x * 2 for x in xs if x >= k])
```

正解コードとの文字列一致は要求しない。同じ入出力関係を実現していれば正解とする。

## 対象外の要素

以下は本課題では扱わない。

- 任意のPythonプログラミング
- クラス、再帰、非同期処理
- ファイルやネットワークアクセス
- NumPy、Pandasなどの外部ライブラリ
- アルゴリズム競技問題
- 複数関数からなるプログラム
- 自由形式の会話
- HumanEvalなど一般的なコード生成ベンチマークでの高性能化

## なぜここまで狭くするのか？

この狭さが本課題の成立条件である。

1. 語彙が閉じるため、トークナイザを4,096語彙まで小さくできる。埋め込みパラメータが減り、モデル全体を小さくできる。
2. **生成されたコードを実際に実行して正誤を機械判定できる。** 評価が完全に自動化され、人手による採点が不要になる。
3. 学習データの全件について、それが正しく動くコードであることを保証できる。

## モデル仕様

|                | **Boku-nano**(必修) | **Boku-mini**(発展) |
| -------------- | ------------------- | ------------------- |
| パラメータ数   | 約1,400万           | 約7,750万           |
| d_model        | 384                 | 768                 |
| 層数           | 8                   | 12                  |
| Attention head数 | 8                 | 12                  |
| KV head数      | 2                   | 3                   |
| head_dim       | 48                  | 64                  |
| d_ff (SwiGLU)  | 1,024               | 2,048               |
| 語彙数         | 4,096               | 4,096               |
| 文脈長         | 256                 | 256                 |
| 学習トークン   | 100〜300M           | —（実測して報告）   |
| 学習時間の目安 | 30分から1.5時間     | —（実測して報告）   |

トークナイザは両モデルで共通の1種類（語彙4,096）を用い、文脈長も両モデルで256に揃える（`## データ規模`のコンテキスト長と一致させる）。1レコードは65〜145 tokensであり、256で十分に収まるため、Boku-miniで文脈長を伸ばす利得はない。これにより、Boku-nanoとBoku-miniを比較する際、変化する条件がモデルサイズ（d_model・層数・ヘッド数）だけになる。

Boku-miniは発展課題であり、必修ではない。学習トークン数は、訓練コーパスの規模と学習時間の実測に応じて学生が決め、実際に使用した値を報告する。

## 教師モデル

教師には、公式の`Qwen/Qwen3-4B-AWQ`を第一候補とする。同モデルは4-bit AWQ版として公開され、モデルページとLICENSEにはApache License 2.0が示されている。[モデルカード](https://huggingface.co/Qwen/Qwen3-4B-AWQ)、[LICENSE](https://huggingface.co/Qwen/Qwen3-4B-AWQ/blob/main/LICENSE)

教師モデルは主として次の用途に使用する。

- 日本語指示の言い換え
- 原子的な操作の説明文生成
- 初学者向け解説の生成
- 同じプログラムに対する表現の難易度変更

前述のとおり、教師モデルに正解プログラムそのものを決めさせてはいけない。正解の意味構造、コード、テストはルールベースで作成し、教師は自然言語（日本語）表現を増やす役割に限定する。

教師生成時には以下を固定・記録する。

- モデル名
- モデルのrevisionまたはコミットID
- 量子化方式
- 推論ライブラリのバージョン
- system prompt
- sampling設定
- seed
- 生成日時
- プロンプトのハッシュ値

思考過程を大量に保存する必要はない。非thinkingモードを使用し、最終的な指示文または解説文だけを保存する。

## データ生成

### 意味表現の生成

自然言語より先に、問題の意味を表す中間表現を生成する。

例（「問題例」の課題に対応する意味AST）：

```json
["ge", "double", "asc"]
```

この中間表現をDSLまたは意味ASTと呼ぶ。

**意味ASTの定義**：意味ASTは「**操作列のみ**」を表し、`k`の値などの具体的な定数は含めない。定数のバリエーションは、後段のサンプル展開（1意味ASTあたり最大20例）で吸収する。この定義により、後述の「意味AST単位での分割」が漏洩防止の基礎として機能する。

ただし、操作の適用順序を自由にしているため（`### 日本語指示の生成`を参照）、**可換な操作対では異なる意味ASTが同一の関数を表す**。意味AST単位の分割だけでは漏洩を防ぎきれないため、`## 訓練・検証・テスト分割`のデータ漏洩検査で`behavior_hash`による確認を併せて行う。

**定数の扱い**：展開例では、`k`をパラメータのまま参照するコードと、`k`をリテラルに具体化したコードの両方を作ってよい。

```python
[x for x in xs if x >= k]    # パラメータのまま
[x for x in xs if x >= 5]    # リテラルに具体化
```

リテラルに具体化する場合は、日本語指示も「k以上の値を残す」ではなく「5以上の値を残す」とする。

**リテラルの値域**：リテラルに使える値は、`k`と同じ**1以上10以下の整数**に限る。こうすると、リテラル版のコードは必ず「参照インタプリタを`k`＝そのリテラル値で評価した結果」と一致するため、パラメータ版とまったく同じ検証機構をそのまま適用できる。値域外のリテラル（0や負数）を許すと、対応する`k`が存在せず参照インタプリタで正誤を判定できなくなる。

この値域のうち、次の値は除外する。リテラルに具体化した結果が、他の操作や恒等変換と一致してしまうためである。

- 恒等変換になる値：`mul_k`に1
- 恒真になる値：`multiple_of`に1
- 他の操作と同一になる値：`multiple_of`に2（`even`と同一）、`mul_k`に2（`double`と同一）、`mul_k`に3（`triple`と同一）、`ge`に1（要素が整数なので`positive`と同一）

`add_k`・`sub_k`・`mul_k`に0、`gt`・`lt`に0といった値は、そもそも1以上10以下に含まれないため、除外規定を必要としない。なお`lt`に1（`x < 1`）は`negative`（`x < 0`）とは異なる（ゼロを含む）ため、除外しない。

なお、リテラルに具体化したコードは引数`k`を参照しないが、シグネチャは変更せず`solve(xs, k)`のまま保つ。

各意味ASTから、独立した二つのプログラムを作る。この二つのプログラムの実装には、開発支援AIを使用してよい。

- 正解を計算する参照インタプリタ
- 学習対象となるPythonコードを出力するコード生成器

両者の出力をランダムテストで比較する。これにより、コード生成器自身のバグを検出する。

### 日本語指示の生成

次の四段階で行う。教師モデルの出力を直接学習データにせず、必ず人間の承認を挟むことが要点である。

1. 教師モデルにそれぞれのアトミックな操作について10〜30種類の日本語表現を生成させる
2. 人間が、生成させたものに意味の変化がないか確認し、承認済み表現辞書を作る
3. ルールベースの文生成器が、意味ASTと承認済み表現を組み合わせる
4. 一部についてのみ、教師モデルに文全体の言い換えを行わせる

**操作の適用順序**：操作の適用順序は自由であり、同じ操作の集合でも順序が違えば別の意味ASTとして扱う。

```
[asc, take_first]   昇順に並べてから先頭k個を取る
[take_first, asc]   先頭k個を取ってから昇順に並べる
```

この二つは異なる出力を持つため、操作が2個以上の意味ASTでは、日本語指示に適用順序を明示する接続表現を必ず含める。承認済み表現辞書には「〜してから〜」「〜した後で〜」「まず〜、次に〜」などの順序マーカーを用意すること。

操作が3個以上の場合は、順序マーカーを連鎖させ、どの操作が何番目かが一意に読み取れるようにする。

```
[even, add_k, desc, take_first]
  まず偶数だけを残し、次にそれぞれにkを加え、
  降順に並べてから先頭k個を取る
```

操作数が増えるほど教師モデルによる意味のずれが起きやすくなるため、difficultyが3以上の指示文は特に注意して承認すること。

例えば「偶数だけを残す」について、次の表現を用意する。

- 偶数の要素だけを抽出する
- 2で割り切れる値のみを残す
- 奇数を取り除く
- 偶数に該当する要素を選ぶ

教師が生成した文には、「以上」が「より大きい」に変わるなど、意味を変える言い換えがあるため、そのまま無条件で採用するべきではない。人間のチェックを必ず入れること。

### 解説文の生成

特殊トークン`<|explanation|>`で始まる解説文レコードを作成する。これは、モデルに日本語の文構造とPythonの語彙を基礎から学ばせるためのデータであり、後述する「全トークンを損失計算の対象にする」データにあたる。

解説文は、**教師モデルが生成したもののみを使用する**。外部から取得した日本語コーパスやPythonの教材テキストは使用しない。これは「フルスクラッチ学習」の定義とライセンス方針の双方を保つためである。

生成手順は次のとおり。

1. 意味ASTと参照コードを教師モデルに与える
2. そのコードが何をしているかの初学者向け解説を日本語で生成させる
3. 日本語指示と同様に、人間が内容の正しさを確認する
4. 承認されたものだけを`explanation`フィールドに保存する

なお、解説文レコードの件数とトークン数の目安は`## データ規模`に含めていない。まずは指示→コードのレコードでパイプライン全体を完成させ、解説文をどの分量で混ぜるかの設計は**残る課題**とする。

### コードの構造的変換

同じ意味ASTから複数の等価コードを生成する。この生成器の実装にも開発支援AIを使用してよい。

- 内包表記と通常の`for`ループ
- 一時変数の有無
- 変数名の変更
- 条件式の順序変更
- `reverse=True`と逆順操作
- 1行の`return`と複数行形式
- コメントおよび型注釈の有無

すべてのコードを実行し、参照インタプリタと同じ結果になる場合だけ採用する。

### データレコード

最低限、次の情報をJSONLなどで保存する。ただし、必ずしもJSONのスキーマを踏襲する必要はない。中身は各学生の判断に任せる。

```json
{
  "spec_id": "task-000001",
  "record_type": "instruction_to_code",
  "semantic_ast": ["ge", "double"],
  "instruction_ja": "...",
  "reference_code": "...",
  "explanation": null,
  "tests": [],
  "difficulty": 2,
  "code_style": "list_comprehension",
  "semantic_hash": "...",
  "text_hash": "...",
  "behavior_hash": "...",
  "teacher_model": "...",
  "teacher_revision": "...",
  "prompt_hash": "...",
  "verification": {
    "syntax_ok": true,
    "ast_safe": true,
    "tests_passed": true
  }
}
```

補足：

- `record_type`は`instruction_to_code`（指示→コード）または`explanation`（解説文）のいずれかとする。損失計算の対象範囲がこの値によって変わる。
- `difficulty`は組み合わせた操作の個数（1〜4）とする。
- `explanation`は解説文レコードでのみ使用し、指示→コードのレコードでは`null`とする。
- `behavior_hash`は、全レコードで共通の固定入力集合に対する参照インタプリタの出力列から計算する。意味ASTが違っても関数として同一なら同じ値になる。用途は`## 訓練・検証・テスト分割`のデータ漏洩検査を参照。

## データ規模

夏休み課題では次を目安とする。

| 段階                             |                目安 |
| -------------------------------- | ------------------: |
| 教師モデルによる直接生成         |      0.5〜3M tokens |
| 生成する意味AST                  | 30,000〜100,000種類 |
| 構造変換後の生コーパス           |     50〜200M tokens |
| 検証・選抜後の訓練コーパス       |     30〜100M tokens |
| 学習中にモデルが見る総トークン数 |    100〜300M tokens |
| コンテキスト長                   |          256 tokens |

意味ASTの上限は、対象操作の数から機械的に決まる。24種類の操作から1〜4個を順序付きで選ぶ場合、組み合わせは次のとおりである。

```
1個   24
2個   24 × 23              =     552
3個   24 × 23 × 22         =  12,144
4個   24 × 23 × 22 × 21    = 255,024
合計                         267,744 種類
```

この全数を生成する必要はない。上表の30,000〜100,000種類は、この空間から抽出した数である。ただし1〜3操作の組み合わせは合計12,720種類しかないため、無作為に抽出すると4操作の問題が大半を占める。難易度の分布を意図した形にするには、`difficulty`ごとの生成件数をあらかじめ決めて層化抽出すること。

```
30,000種類を生成する場合の配分例
  difficulty 1        24 種類（全数）
  difficulty 2       552 種類（全数）
  difficulty 3    12,144 種類（全数）
  difficulty 4    17,280 種類（255,024 種類から抽出）
  合計            30,000 種類     1〜3操作 42% / 4操作 58%
```

トークン数の目安は、この意味AST数と展開例数から逆算したものである。中央的な設定を代入して確認する。なお、上表のうち生コーパス・訓練コーパス・総トークン数の3行と以下の検算は、いずれも**指示→コードのレコードのみ**を数えた値である。解説文レコードは`### 解説文の生成`に記したとおり本表に含めていない（「教師モデルによる直接生成」の行は、承認済み表現辞書と言い換えの生成量を指す）。

```
意味AST                  30,000〜100,000 種類
1意味ASTあたりの展開例数   最大 20 例
1レコードの長さ           65〜145 tokens
  （日本語指示 30〜55 + コード 30〜85 + 特殊トークン 5）

中央的な設定での確認
  65,000 種 × 20 例         = 1,300,000 レコード
  1,300,000 × 105 tokens    =       137M tokens   ← 生コーパスの帯に収まる
  選抜通過率 50% と仮定      =        68M tokens   ← 訓練コーパスの帯に収まる
  68M × 3 epoch             =       204M tokens   ← 総トークン数の帯に収まる
```

同じデータを複数epoch使用してよい。ただし、「固有トークン数」と「学習中に見た延べトークン数」を分けて報告すること。

## データの検証と選抜

次の条件をすべて通過したレコードだけを訓練候補とする。

1. `ast.parse`に成功する
2. 許可されていない構文を含まない
3. import、属性アクセス、ファイル操作などを含まない
4. 制限時間内に実行できる
5. 参照インタプリタと全テストで一致する
6. 関数名`solve`と位置引数`(xs, k)`を守る
7. 長さ上限を超えない
8. 完全重複コードでない
9. 同じ意味ASTにデータが偏りすぎていない

選抜では、複雑な品質モデルを作る必要はない。以下の単純な規則でよい。

- 操作数（`difficulty`）の分布は、`## データ規模`で決めた層化抽出の配分をそのまま保つ。**難易度を均等にはしない。** `difficulty` 1 は意味ASTが24種類しかなく、1意味ASTあたり最大20例なら480レコード（約5万トークン）が上限である。4水準を均等に揃えると訓練コーパス全体が約0.2M tokensに制限され、データ規模表の30〜100M tokensに二桁以上届かなくなる
- 各演算子の出現頻度を均す
- コード形式を均す
- 日本語テンプレートを均す
- 一つの意味ASTにつき最大20例などの上限を設ける

## 訓練・検証・テスト分割

データは日本語文やコードを生成する前に、意味AST単位で分割する。**意味ASTを分割した後で言い換えやコード変換を行わないと、同じ問題の表記違いが訓練とテストに混入する**ため、順序を守ること。

### 分割手順

単純なランダム分割だけでは、後述の組合せ汎化テストが成立しない。訓練側に同じ操作の組合せが混ざってしまうためである。そこで、次の四つの手順で分割する。

```
手順1  組合せ汎化テスト用に、操作ペアをN組予約する
       例: (even × desc), (abs × take_first) ...

手順2  予約ペアを含む意味ASTを全て訓練・検証から除外し、
       組合せ汎化テスト集合とする
       ※ 単体操作 (even単体, desc単体) は訓練側に残す

手順3  残った意味ASTを、意味AST単位で 80 / 10 / 10 に分割する
       訓練 80% / 検証 10% / 通常テスト 10%

手順4  分割が確定した後で、日本語の言い換えとコード変換を実行する
```

手順1で予約するペアの単体操作を訓練側に残すことが重要である。これにより「個々の操作は学習したが、その組合せは見ていない」という状況を作れる。

**予約するペア数N**：3〜5組を目安とする。1組予約するごとに、そのペアを含む意味ASTがすべて訓練・検証から外れる。24操作から4個を順序付きで選ぶ場合、特定のペアを含む意味ASTは全体の約2.2%（3操作なら約1.1%、2操作なら約0.4%）にあたり、`## データ規模`の30,000種類の配分例では**1ペアあたり約510種類**が除外される。

```
N = 3   約1,500種類（全体の約5%）が組合せ汎化テストへ
N = 5   約2,450種類（全体の約8%）が組合せ汎化テストへ
```

Nが1〜2組では、測定結果が選んだペアの性質に左右されやすい。逆にNを10組まで増やすと除外が約4,700種類（約16%）に達し、組合せ汎化テストが通常テスト（残りの10%）を上回るうえ訓練データも削れる。3〜5組であれば、訓練集合を9割以上残したまま、`## ゴールとして確認すべきこと`の「100問以上」を十分に満たせる。

予約ペアは異なるカテゴリの組合せ（抽出×並べ替え、変換×切り出しなど）から選び、汎化の種類が偏らないようにする。実際に予約したペアと、除外された意味ASTの件数を報告すること。

### テスト集合の種類

**通常テスト**

手順3で通常テスト側に割り当てられた意味ASTを使用する。意味AST単位で分割しているため、操作列そのものは訓練に出現しない。ただし予約ペアを含まないため、組合せ汎化テストのように「意図的に未学習にした組合せ」は含まない。日本語テンプレートと`k`の値も、訓練と重複しないものを用いる。

**言い換えテスト**

訓練に使用しなかった日本語テンプレートを使用する。

**組合せ汎化テスト**

手順1で予約した組合せを使用する。例えば、訓練では「偶数抽出」と「降順整列」を別々に見せ、テストでは両者を組み合わせる。

**境界値テスト**

空リスト、要素1個、すべて不合格、すべて同値、負数を含むリストなどを使用する。

### データ漏洩検査

分割後、次の検査を行い、結果を報告する。

1. 訓練集合とテスト集合の`semantic_hash`に重複がないこと
2. 訓練集合とテスト集合の`text_hash`に重複がないこと
3. 予約した操作ペアが訓練集合の意味ASTに一つも出現しないこと
4. 訓練コードとテスト正解コードの間に、長い文字列一致（例: 正規化後の連続40文字以上、またはトークン列の長い n-gram 一致）が存在しないこと
5. 訓練集合とテスト集合の`behavior_hash`に重複がないこと

第5項は、操作の適用順序を自由にしていることの帰結として必要になる。**可換な操作対では、異なる意味ASTが完全に同じ関数を表す。**

```
[even, positive] と [positive, even]   フィルタ同士は可換
[even, desc]     と [desc, even]       フィルタと整列も可換
[double, triple] と [triple, double]   どちらも ×6
```

これらは`semantic_hash`が異なるため第1項を通過し、生成コードも文字列としては異なるため第4項と`## データの検証と選抜`の「完全重複コードでない」も通過してしまう。しかしモデルにとっては同一の課題であり、訓練とテストに分かれると通常テストの数値が実際より高く出る。`[square, abs]`が`[square]`と一致する（二乗は常に非負なので絶対値が恒等になる）ような意味的な縮退も、同じ検査で検出できる。

`behavior_hash`は、境界値と乱数を混ぜた固定の入力集合（例: 境界値20件＋ランダム50件）に対する参照インタプリタの出力列を連結してハッシュ化すればよい。入力集合は全レコードで共通のものを使う。重複が見つかった場合は、テスト側のレコードを除外する。

## トークナイザ

訓練データだけを用いて、BPEまたはUnigramトークナイザを作成する。Boku-nanoとBoku-miniで共通の1種類を使用する。

推奨設定：

- 語彙数：4,096
- byte fallback：有効
- 日本語とPythonコードを同一語彙で扱う
- 特殊トークンを明示する

例：

```text
<|pad|>
<|bos|>
<|eos|>
<|task|>
<|code|>
<|explanation|>
```

## 必修モデル：Boku-nano

推奨構成は約1,400万パラメータのDecoder-only Transformerである。

| 項目             |       推奨値 |
| ---------------- | -----------: |
| 層数             |            8 |
| d_model          |          384 |
| Attention head数 |            8 |
| KV head数        |            2 |
| head_dim         |           48 |
| FFN size (d_ff)  |        1,024 |
| 語彙数           |        4,096 |
| 最大系列長       |          256 |
| 位置表現         |         RoPE |
| 正規化           |      RMSNorm |
| 活性化           |       SwiGLU |
| Attention方式    | GQA (8:2 = 4:1) |
| 入出力埋め込み   | weight tying |

Attention head数8に対しKV head数2のGrouped Query Attention（GQA）を採用する。比率は4:1で、KVキャッシュはMulti-Head Attentionの4分の1になる。Boku-mini（head 12 / KV head 3）、Boku-1B（head 16 / KV head 4）も同じ4:1であり、三モデルすべてで同一のGQA比率になる。これにより「Boku-1Bの予備実験」という本課題の位置づけが、Attention構造の面でも成立する。

なお、この構成では`head_dim`が384 ÷ 8 = **48**となり、64や128という一般的な値から外れる。これは誤記ではなく意図した設計である。d_model 384 のまま4:1を満たす構成は7通りあるが、KV次元がd_model ÷ 4 = 96に固定されるためどれを選んでもパラメータ数は同一であり、選択はhead_dimの大きさだけの問題になる。head 4（head_dim 96）ではKVヘッドが1個になりGQAではなくMQAになってしまい、head 12以上ではhead_dimが32以下まで小さくなる。head 8 は、GQAとして成立する範囲でhead_dimを最大に取れる構成である。head_dim 48 は偶数なのでRoPEが適用でき、8の倍数なのでBF16のテンソルコアやFlashAttentionの制約も満たす。

（d_model 384 で head_dim 64 と 4:1 を同時に満たすことはできない。8 × 64 = 512 ≠ 384 となるためである。）

### パラメータ数の内訳

総パラメータ数を報告する際は、次のように内訳を示すこと。

```
埋め込み (weight tying)     4,096 × 384                    =  1,572,864

1層あたり                   （KV次元 = KV head 2 × head_dim 48 = 96）
  W_q                       384 × 384                      =    147,456
  W_k                       384 × 96                       =     36,864
  W_v                       384 × 96                       =     36,864
  W_o                       384 × 384                      =    147,456
  W_gate                    384 × 1,024                    =    393,216
  W_up                      384 × 1,024                    =    393,216
  W_down                    1,024 × 384                    =    393,216
  RMSNorm × 2               384 × 2                        =        768
  小計                                                       1,549,056

8層                         1,549,056 × 8                  = 12,392,448
最終RMSNorm                 384                            =        384

合計                                                        13,965,696
```

ライブラリの都合に合わせて多少変更してよいが、その場合も同様に総パラメータ数を算出して報告する。

### 推奨学習設定

- 数値形式：BF16
- Optimizer：AdamW
- 最大learning rate：`3e-4`
- warmup：全stepの3%
- schedule：cosine decay
- weight decay：`0.1`
- gradient clipping：`1.0`
- global batch：32,768 tokens前後
- 学習量：100〜300M tokens
- 総step数：約3,000〜9,000 step（100〜300M tokens ÷ 32,768 tokens/step）
- 評価間隔：100〜250 step
- checkpoint：500 stepごと
- generation評価：temperature 0（貪欲デコード）、最大192 tokens

系列長256、micro batch 8、gradient accumulation 16とすれば、global batchは 256 × 8 × 16 = 32,768 tokensになる。

### 損失計算の対象範囲

損失計算の対象は、レコードの種類によって変える。

- `record_type` が `instruction_to_code` のレコード：`<|code|>`以降のトークンのみを対象とする
- `record_type` が `explanation` のレコード：全トークンを対象とする

#### 指示→コードのレコード

日本語指示からコードを生成する訓練例では、`<|code|>`以降の出力部分だけを損失関数の計算対象とする。

例えば、訓練データが次の形だとする。

```text
<|task|>
整数リストの各要素を2倍してください。
<|code|>
def solve(xs, k):
    return [x * 2 for x in xs]
<|eos|>
```

言語モデルは、各位置で「次のトークンは何か」を予測し、その予測が正解からどれだけ外れたかをCross Entropy Lossで測る。ここで、`<|code|>`以降だけを対象にする場合、損失計算のイメージは次のようになる。

```text
<|task|> 整数リストの各要素を2倍してください。 <|code|> def solve ...
   無視       無視          無視                  計算対象 →
```

つまり、日本語の指示と、それまでに出力したコードを読んで、正しい次のコードトークンを予測できるかだけでlossを計算する。実装上は、損失を計算しない位置のラベルを、PyTorchで一般的に使われる`-100`に設定すればよい。

もし指示部分にも損失を計算すると、モデルは日本語の問題文そのものを予測する機能とコーディング機能の両方を学習してしまう。一方、コード部分だけに損失を計算すると、学習能力を主に後者へ集中させることができる。

なお、指示部分の損失を無視しても、モデルが指示を読まなくなるわけではない。トークンとしては正しく内部表現に変換され、学習される。「問題部分の予測をしなくなる」という点が重要である。

#### 解説文のレコード

教師モデルが生成した解説文（`<|explanation|>`レコード）では、全トークンを損失計算の対象とする。

```text
<|explanation|>
Pythonのリスト内包表記は、リストを簡潔に作る構文です。例えば……
<|eos|>
← 全体を損失計算の対象にする →
```

これは、モデルに日本語、Pythonの語彙、説明文の構造などを基礎から学ばせるためである。前述のとおり、このデータは教師モデルが生成し人間が承認したものに限り、外部コーパスは使用しない。

## 評価指標

モデルの評価では、BLEUや文字列一致よりも実行結果を重視する。

| 指標              | 内容                                                  |
| ----------------- | ----------------------------------------------------- |
| Validation loss   | 学習が進んだか                                        |
| Syntax-valid率    | `ast.parse`に成功した割合                             |
| Safe-AST率        | 禁止構文を含まない割合                                |
| Signature-valid率 | 関数名`solve`・位置引数`(xs, k)`を正しく定義した割合  |
| Executable率      | timeoutや例外なしで実行できた割合                     |
| pass@1            | 1回の生成（temperature 0）で全hidden testに合格した割合 |
| 組合せ汎化率      | 未学習の演算組合せに対するpass@1                      |
| 最大GPUメモリ     | 学習時のピークGPUメモリ（実行可能性の記録）           |
| tokens/s          | 学習スループット（再現性と計算効率の記録）            |

Signature-valid率の判定基準は次のとおりとする。型注釈の有無は問わない。

```text
✓ def solve(xs, k):
✓ def solve(xs: list[int], k: int) -> list[int]:
✓ def solve(xs: list, k: int):
✗ def solve(xs):
✗ def solve(xs, k, n=0):
✗ def f(xs, k):
```

生成評価はすべてtemperature 0の貪欲デコードで行う。したがって同一入力に対する出力は決定的であり、指標はpass@1に統一する。

テスト問題ごとに20個以上のランダム入力を生成する。コード作成時に使用したテストとは異なるhidden testで判定する。

## 比較実験

最低一つ、可能なら二つの比較実験を行う。

推奨候補：

- 学習前のランダムモデル対学習後モデル
- 10M tokens対100M tokens
- 教師言い換えなし対あり
- コード表現1種類対複数種類
- Boku-nano(約1,400万)対Boku-mini(約7,750万)
- 重複除去前対重複除去後
- ランダム分割対意味AST分割

比較では一度に一つの条件だけを変更する。モデルサイズの比較を行う場合、トークナイザ・訓練データ・文脈長は共通のものを使用すること。

## ゴールとして確認すべきこと

本課題の完了条件は次のとおりである。

- データ生成から評価まで、一つの手順で再実行できる
- トークナイザとモデルがランダム初期値から作成されている
- 訓練lossと検証lossの推移を提示できる
- 学習前後の出力を比較できる
- 教師モデルなしで学生モデル単独の推論ができる
- 未学習の意味ASTに対して100問以上評価している
- モデルが生成したコードのうち、少なくとも5問がhidden testを完全通過する
- 成功例だけでなく、最低20件の失敗を分類している
- データ漏洩検査（5項目）の結果を報告している
- 使用データ、モデル、ソフトウェアの由来を記録している

以下は成績評価の基準ではなく、パイプラインが正常に機能しているかを判断するための健康状態の参考値である。

- Syntax-valid率 80%以上
- 通常テストのpass@1 30%以上

これらに達しなかった場合も、原因を切り分ける比較実験が十分であれば評価対象とする。ただし、最終デモまでには少数でも実行成功例を得ること。

## 学ぶ内容

本課題を通じて、学生は次を学ぶ。

- 自己回帰言語モデルの学習原理
- トークナイザと語彙設計
- Transformerのモデルサイズと計算量
- teacher inferenceとstudent trainingの違い
- response-based distillationの考え方
- 合成データにおける品質と多様性の関係
- train/test leakageの危険性
- 実行結果によるコード評価
- データ重複と難易度分布
- BF16、Gradient Accumulation、Checkpointing
- GPUメモリと学習速度の測定
- 再現可能な機械学習実験
- モデル、データ、ソフトウェアの由来管理
- 失敗例から仮説を作る研究方法

## 発展課題：Boku-1B

本課題を完了した時点で、「Boku-1B」の予備実験を終えた状態にある。以下は発展の方向であり、卒業研究のテーマになり得る。

約1Bパラメータの例：

| 項目             |    値 |
| ---------------- | ----: |
| 層数             |    22 |
| d_model          | 2,048 |
| Attention head数 |    16 |
| KV head数        |     4 |
| FFN size (d_ff)  | 5,504 |
| 語彙数           | 8,192 |
| 最大系列長       |   256 |

この構成のパラメータ数は約9.9億である。

Boku-1Bの語彙数だけが8,192となっているのは、Boku-1Bが本課題の閉じたドメイン（`solve`関数のみ）を超えた、より広い日本語・コーディング領域を対象とするためである。Boku-1Bに着手する際は、対象ドメインの再定義とあわせて語彙サイズを設計し直す。

このモデルもランダム初期値から学習する。ただし、夏休み中に十分な事前学習を完了させることは要求しない。

推奨する範囲は次のとおり。

- まず10M tokensの動作確認
- 次に50〜200M tokensの短期学習
- Boku-nanoとloss、生成例、速度を比較
- 学習不足の1Bモデルが小型モデルより良いとは限らないという点を、実測で検証する

## ライセンスと来歴管理

Apache License 2.0のQwen3公式モデルは教師候補として扱いやすいが、モデルのライセンスだけで、すべての生成物について第三者の権利が存在しないことまで保証されるわけではない。そこで、以下のような情報を記録しておく。

- 使用したモデルのLICENSEを保存する
- 正確なモデルrevisionを固定する
- 教師プロンプトと生成条件を保存する
- インターネットから取得したコードを合成データへ混入させない
- 既存コードとの長い一致を検査する
- 自作テンプレートとルール生成物を中心にする
- `THIRD_PARTY.md`を作成する
- Dataset CardとModel Cardに生成方法を明記する

開発支援AIを使用した場合は、どの範囲の実装に使用したかを併せて記録する。ただし、前述のとおり開発支援AIの出力は生成器のソースコードであり、合成データそのものではない。

## 研究課題への発展

本実習から、次の独立した研究テーマへ発展できる。

- どの構造変換が組合せ汎化を改善するか
- 日本語言い換えの多様性と実行精度の因果関係
- 意味重複を残す場合と除去する場合の比較
- 難易度カリキュラムの効果
- 合成コードの表面的多様性と意味的多様性の違い
- 教師モデルサイズと学生性能の関係
- 実行フィードバックを用いた反復的データ改善
- 失敗例を重点的に追加するactive data generation
- 14M、77M、300M、1B間の小規模スケーリング則

## 付録: 参照すべき文献・資料

- Vaswani et al., _Attention Is All You Need_ (2017)
- Hoffmann et al., _Training Compute-Optimal Large Language Models_ (2022)
- Kaplan et al., _Scaling Laws for Neural Language Models_ (2020)
- Su et al., _RoFormer: Enhanced Transformer with Rotary Position Embedding_ (2021)
- Shazeer, _GLU Variants Improve Transformer_ (2020)
- Zhang & Sennrich, _Root Mean Square Layer Normalization_ (2019)
- Ainslie et al., _GQA: Training Generalized Multi-Query Transformer Models from Multi-Head Checkpoints_ (2023)
- Chen et al., _Evaluating Large Language Models Trained on Code_ (2021)
- Apache License, Version 2.0 本文
