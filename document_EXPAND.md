# 実装計画：Boku-nano Phase 1 後半(2)（展開・生成器・検証と選抜）

## Context

`document_SPLIT.md` が分割を確定させた後の工程を扱う。入力は `data/split/splits.jsonl`、
出力は学習に使えるレコード（指示→コード、および解説文）である。

改訂版 L482「分割が確定した後で、日本語の言い換えとコード変換を実行する」の**後段**にあたる。
分割の設計は本書では扱わない。**分割を再計算しない**こと（`document_SPLIT.md` §2.5）。

```
splits.jsonl（訓練34,040 / 検証4,255 / 通常テスト943 / 組合せ汎化1,425）
   ↓ バインディング展開      §2.1
   ↓ コード生成 × 構造変換   §2.3 §2.4
   ↓ 日本語指示生成          §2.5（承認済み表現辞書 §2.6 §2.7 が前提）
   ↓ 解説文レコード          §2.8
   ↓ 検証9項目 → 選抜        §2.10 §2.11
   ↓ 漏洩検査 段2（第2・4項） §2.12
```

### 規範とする文書

1. `芦原・倉林研究室_..._改訂版.md` — 唯一の正。本書の L… はすべてこれ
2. `document_AST.md` §2.4（一様具体化の制約）、§5（`ja` 生成器への申し送り）、§10（残したフック）
3. `document_SPLIT.md` §8（申し送り）
4. `docs/open_questions.md` ⑩⑪（リテラル具体化と「最大20例」の解釈）

### 今回作るもの

| モジュール | 役割 | 依存 |
| --- | --- | --- |
| `boku/expand/binding.py` | バインディングの列挙と例数配分 | 標準ライブラリのみ |
| `boku/codegen/emit.py` | 意味AST → Pythonソース（`CodeGenerator` の実装） | 同上 |
| `boku/codegen/styles.py` | 構造変換7軸の定義 | 同上 |
| `boku/ja/render.py` | 意味AST → 日本語指示（`InstructionRenderer` の実装） | 同上 |
| `boku/ja/phrasebook.py` | 承認済み表現辞書の読み込みと引き当て | 同上 |
| `boku/verify/checks.py` | 検証9項目（L443-451） | 同上 |
| `boku/verify/select.py` | 選抜規則（L455-461） | 同上 |
| `scripts/build_dataset.py` | 実行の入口 | ＋ hydra, mlflow |
| `scripts/teacher/generate_phrases.py` | 教師モデルによる日本語表現の生成 | ＋ 推論スタック |
| `scripts/teacher/approve.py` | 人間の承認フロー（CLI） | ＋ hydra |
| `data/ja/approved_phrases.yaml` | **承認済み表現辞書**（追跡する。人間の判断の記録） | — |

### 今回作らないもの

トークナイザ（Phase 2）、モデル・学習・評価（Phase 3〜5）、hidden test の入力生成器（Phase 4）、
コード→ASTパーサ（`docs/canonical_forms.md` に置いたまま）。

---

## 1. データ規模の検算が分割と噛み合っていない（`target` を 45,000 に上げて対処した）

**これは仕様の食い違いの報告と、それに対する人間の判断の記録である。**
`CLAUDE.md` の規定に従い、勝手にどちらかへ寄せず報告し、判断を仰いだ（2026-08-04、決着済み）。

改訂版 L424-437 の検算は、生成した意味AST**全数**に展開例数を掛けている。

```
65,000種 × 20例 = 1,300,000レコード × 105 tokens = 137M   ← 生コーパスの帯 50〜200M ✓
選抜通過率 50%                                    = 68M    ← 訓練コーパスの帯 30〜100M ✓
68M × 3 epoch                                     = 204M   ← 総トークンの帯 100〜300M ✓
```

しかし L465 は「日本語文やコードを生成する前に、意味AST単位で分割する」と定めている。
展開されるのは全数ではなく、**訓練集合に割り当てられた分だけ**である。本実装の実測値を入れると：

| | 意味AST | 生コーパス | 選抜50% | 3 epoch |
| --- | ---: | ---: | ---: | ---: |
| L424-437 の検算（分割を無視） | 65,000 | 136.5M ✓ | 68.2M ✓ | 204.8M ✓ |
| 旧 target=30,000 全数 | 30,000 | 63.0M ✓ | 31.5M ✓ | 94.5M ✗ |
| 旧 target=30,000 の**訓練集合のみ** | 22,806 | 47.9M ✗ | 23.9M ✗ | 71.8M ✗ |
| **採用：target=45,000 の訓練集合** | **34,040** | **71.5M ✓** | **35.7M ✓** | **107.2M ✓** |

`target=30,000` では、分割後に3つの帯すべてを下回っていた。帯に乗せる方法は3つあった。

| 対処 | 必要な値 | 判断 |
| --- | --- | --- |
| A. 選抜通過率を上げる | 62.6%以上 | **採らない。**通過率を事後の実測に賭ける形になり、下回ったら結局 `target` の変更＝コーパス再構築に戻る |
| **B. `target` を上げる** | **45,000**（3 epoch で3帯すべてを満たす最小値） | **採用**（2026-08-04） |
| C. 帯の解釈を変える | 訓練コーパスの帯を目安と扱う | **採らない。**根拠が弱い |

40,000 でも訓練コーパスの帯（31.9M）には乗るが、3 epoch が 95.8M で総トークン帯の下限100Mに
届かない。**45,000 が3つの帯すべてを満たす最小の値である。**

### 副作用：difficulty 4 の比率が上がる（受け入れて記録する）

difficulty 1〜3 は全数 12,720件で頭打ちなので、`target` を増やした分はすべて difficulty 4 に乗る。

| | difficulty 1〜3 | difficulty 4 |
| --- | ---: | ---: |
| target=30,000（L416-421 の配分例） | 42.4% | 57.6% |
| **target=45,000（採用）** | **28.3%** | **71.7%** |

L457 が求めているのは「層化抽出の配分をそのまま保つ」ことであり、特定の比率を要求していない。
**偏りは受け入れ、`manifest.json` と報告書に記録する**（2026-08-04 の判断）。
評価は difficulty 別に出し、4操作に寄ったデータでの結果であることを明示する。

`conf/config.yaml` の `target` を 45,000 にした。`boku/semantics/enumeration.py` の
`DEFAULT_TARGET` は 30,000 のまま置いてある——あちらは L416 の配分例の写しであり、
運用上の既定値は conf が持つ（`document_AST.md` §11 の依存の層）。

---

## 2. 確定した設計判断

### 2.1 バインディング生成器（L269-279、`document_AST.md` §2.4）

バインディングは2形態しかない。**混在は型として表現できない**（`Binding = int | None`）。

1. **パラメータ版**（`None`）：全スロットを `k` のまま。検証は任意の `k` で行える
2. **リテラル版**（`int`）：全スロットを同一の値 `v` に具体化。検証は `k = v` で行う

使える `v` は `uniform_literal_domain`（各スロット値域の共通部分）の要素に限る。
`SemanticAST.is_valid_binding()` が既にこの判定を持っているので、**展開層は値域を再計算しない。**

L269 の等価性がここに効く。リテラル版のコードは「参照インタプリタを `k` ＝そのリテラル値で
評価した結果」と一致するので、パラメータ版とまったく同じ検証機構が使える。

#### 展開の予算（実測）

コーパス45,000件を、バインディングで作れる通り数で分類した。

| バインディング通り数 | 件数 | 20例に必要な「日本語表現 × コード形式」 |
| ---: | ---: | ---: |
| **1**（`k` 参照opなし） | **5,433** | **20通り** |
| 8（共通部分7値＋パラメータ版） | 7,019 | 3通り |
| 9 | 6,107 | 3通り |
| 10 | 5,393 | 2通り |
| 11（共通部分10値＋パラメータ版） | 21,048 | 2通り |

**`k` 参照opを持たない 5,433件が制約を決める。**バインディングによる多様性がゼロなので、
日本語表現 × コード形式だけで20例を埋める必要がある（`document_AST.md` §2.4 の申し送り）。

difficulty 別に見ると、この群は難易度が低いほど濃い（比率は `target` に依存しない）。

| difficulty | 件数 | うち `k` 参照opなし |
| ---: | ---: | ---: |
| 1 | 24 | 14（58.3%） |
| 2 | 552 | 182（33.0%） |
| 3 | 12,144 | 2,184（18.0%） |
| 4 | 32,280 | 3,053（9.5%） |

**設計制約として確定する。**

- 日本語表現は **op あたり最低10種類**（L292 の下限）
- コード形式は **最低4種類**を実装する（§2.4）

10 × 4 = 40通り ≥ 20 で下限を満たす。コード形式が2種類しかないと余裕がゼロになり、
1つでも組合せが検証で落ちると20例に届かない。**4種類を下限とする根拠がこれである。**

#### 「最大20例」は展開時の上限と読む（既決）

`docs/open_questions.md` ⑪ のとおり。L428 と L461 のどちらとも読めるが、本実装は展開時の上限として扱う。
上の予算表はこの読みに依存する。選抜時のみの上限と読むなら、5,433件の制約は消える。

#### 例数の配り方

20例を「バインディング × 日本語表現 × コード形式」の直積から選ぶとき、**軸を独立に均す。**
1つの軸に偏ると L458-460「各演算子の出現頻度を均す／コード形式を均す／日本語テンプレートを均す」に反する。

ラウンドロビンで割り当てる。バインディング通り数が11なら 11 × 2形式 = 22 から20を取るのではなく、
各バインディングに日本語表現とコード形式を巡回で振り、**同じ (ja表現, 形式) の組が同一AST内で
重複しない**ようにする。乱数は展開専用の名前空間から取り、`ast_id` で決定的に派生させる
（再現性のため。分割・固定入力集合・hidden test のいずれとも分ける）。

### 2.2 テンプレートを訓練とテストで分ける（L502, L506）

言い換えテスト（L506）は「訓練に使用しなかった日本語テンプレート」を使う。したがって
**日本語表現には安定したIDを振り、ID集合を訓練用とテスト用に分割する。**

- 承認済み表現辞書の各表現に `phrase_id`（op名＋連番）を持たせる
- op ごとに、表現の 80% を訓練用、20% を言い換えテスト用に固定して分ける
- op あたり10表現なら 8 / 2。**言い換えテスト用に最低2表現を残す**ため、
  op あたり10表現は下限であって目標ではない（15〜20を推奨する）

L502「`k` の値も訓練と重複しないものを用いる」は、`k` が1〜10しかない以上（L104）全体では
成立しない。**同一意味AST内での重複を避ける制約**と読む（`document_SPLIT.md` §7 の解釈15）。
通常テストの意味ASTは訓練に出現しないので、実効的に効くのは言い換えテストだけである。

### 2.3 コード生成器（L281-286, L340-352）

`boku/codegen/__init__.py` の Protocol をそのまま実装する。**宣言は変更しない。**

```python
def emit(self, ast: SemanticAST, binding: Binding, style: CodeStyle) -> str
```

#### 参照インタプリタと独立に書く（L281-286）

**`boku/interp/ops.py` を import しない。レジストリから意味を取らない。**
改訂版 L281-286 が「各意味ASTから、独立した二つのプログラムを作る」ことを求め、両者の出力を
ランダムテストで比較してコード生成器のバグを検出する仕掛けを置いている。実装を共有すると
同じように間違えるので差分テストが素通りする。レジストリに意味を置かなかった設計
（`document_AST.md` §2.8）はこのためにある。

コード生成器は op ごとに**Pythonソースの断片テンプレート**を持つ。参照インタプリタは
Pythonの関数として意味を持つ。**表現形式が違うので、同じ誤解を再現しにくい。**

#### パイプラインの合成

意味ASTは操作列なので、コードは前の結果を次に渡す形になる。

```python
def solve(xs, k):
    ys = [x for x in xs if x % 2 == 0]   # even
    ys = sorted(ys, reverse=True)         # desc
    return ys[:k]                         # take_first
```

`k` 参照opは `binding` が `None` なら `k`、`int` なら**その数値リテラル**を埋める。
リテラル版でも**シグネチャは `solve(xs, k)` のまま保つ**（L279）。引数 `k` が未使用になるが、
L98 が固定するのは関数名と位置引数の2点だけなので違反しない。

#### 受け入れ条件（L352）

**生成した全てのコードを実行し、参照インタプリタと同じ結果になる場合だけ採用する。**

```python
# パラメータ版
eval_solve(emit(ast, None, style))(xs, k) == run(ast.ops, xs, k)      # 任意の k で
# リテラル版（binding = v）
eval_solve(emit(ast, v, style))(xs, k)    == run(ast.ops, xs, v)      # k は何でもよい
```

### 2.4 構造変換は7軸の直交ビットで表す（L344-350）

改訂版が挙げる7項目を、独立に on/off できる軸として定義する。

| 軸 | off | on |
| --- | --- | --- |
| `loop` | 内包表記 | `for` ループ |
| `tmp` | 一時変数なし（式を連ねる） | 一時変数あり |
| `name` | `ys` | `out` / `result`（巡回） |
| `cond` | 条件式の既定順 | 順序を入れ替える |
| `rev` | `reverse=True` | `sorted(...)[::-1]` |
| `multiline` | 1行 `return` | 複数行 |
| `annot` | 型注釈・コメントなし | 型注釈とコメントあり |

`code_style` はこの7ビットを固定順に並べた文字列（例 `"loop=0,tmp=1,name=ys,..."` または7桁のフラグ）で
記録する。レコードの `code_style` フィールド（L368）に入り、L459「コード形式を均す」の集計キーになる。

**全128通りを実装しない。**軸には依存関係がある（`rev` は `order` カテゴリのopがないと意味がない、
`loop` off で `multiline` on は形が限られる）。**最低4種類、目標8種類**の有効な組合せを
`styles.py` に列挙し、ASTごとに適用可能なものを選ぶ。

型注釈を等価な表現として扱えるのは、L98 が固定するのを「関数名が `solve`」「位置引数が
`xs`, `k` の2個」の二点に限っているため（L694-702 の Signature-valid 率の判定基準とも一致する）。

### 2.5 差分ランダムテスト（L286）

コード生成器のバグを検出する主要な仕掛け。**テストとして常設する。**

- 意味AST空間から無作為に選んだASTについて、全 `style` × 全 `binding` を生成
- `boku/limits.py` の条件（L102-105）内でランダムな `(xs, k)` を多数生成
- `eval_solve(code)(xs, k)` と `run(ops, xs, k)` を比較
- 境界（空リスト、要素1個、全要素同値、負数、`k` が長さを超える）を必ず含める

前半で「テストが通っても意味が正しいとは限らない」（`HANDOFF.md` §7）に当たっている。
**参照インタプリタ側の手書き期待値（`test_interp_ops.py`）と、この差分テストの二本立て**で守る。

コードの実行は検証9項目（§2.10）の1〜3を**通してから**行う。順序を逆にしない。

### 2.6 日本語生成器と承認済み表現辞書（L288-323）

`boku/ja/__init__.py` の Protocol をそのまま実装する。生成器は**ルールベース**であり、
教師モデルを実行時に呼ばない（L292 の第3段）。

#### 辞書は3カテゴリを別に持つ

| カテゴリ | 内容 | 引き当てキー |
| --- | --- | --- |
| 1. op別の表現 | 「偶数の要素だけを抽出する」「2で割り切れる値のみを残す」（L316-321） | `OpSpec.ja_key` |
| 2. **順序マーカー**と連鎖規則 | 「〜してから〜」「〜した後で〜」「まず〜、次に〜」 | `difficulty` |
| 3. リテラルのスロット表現 | `k` のまま →「k以上」、リテラル5 →「5以上」（L267） | `binding` |

#### 順序マーカーが品質を決める（L297-314）

操作の適用順序は自由なので、**操作が2個以上の意味ASTでは適用順序を明示する接続表現を必ず含める**
（L304）。3個以上では連鎖させ、どの操作が何番目かが一意に読み取れるようにする（L306）。

```
[even, add_k, desc, take_first]
  まず偶数だけを残し、次にそれぞれにkを加え、降順に並べてから先頭k個を取る
```

順序マーカーが無いと `[asc, take_first]` と `[take_first, asc]` が同じ指示文になり、
**一つの指示に二つの正解が生じる。**既定配分では difficulty 4 が全体の58%を占めるため、
連鎖の品質がデータ全体の品質を決める。

**機械的なガードを置く。** `difficulty >= 2` の生成物に順序マーカーが1つも含まれなければ
エラーにする（テストで固定する）。「注意して承認する」（L314）だけに頼らない。

#### 一指示一正解の確認

同じ (意味AST, binding) から生成した指示文が、別の意味ASTの指示文と文字列一致しないことを
検査する。一致したら順序マーカーの設計に穴がある。**衝突件数を報告する。**

### 2.7 教師モデルの運用（`Qwen/Qwen3-4B-AWQ`、L215-240, L288-295）

教師モデルの役割は**日本語表現を増やすことだけ**である（L226）。正解の意味構造・コード・テストは
ルールベースで作る。教師に正解プログラムを決めさせてはいけない（L53-55）。

#### 実行環境（2026-08-04 に確認）

以前はドライバ版の不整合で `nvidia-smi` が失敗していたが、**再起動で解消した。**

| | |
| --- | --- |
| GPU | NVIDIA GeForce RTX 5090 |
| VRAM | 32,607 MiB |
| ドライバ / CUDA | 595.84 / 13.2 |
| compute capability | **12.0（sm_120、Blackwell）** |

**VRAM は潤沢である。**Qwen3-4B-AWQ は4bit重みで約3GB、量子化しない bf16 でも約8GBなので、
どちらでも収まる。**AWQ を使うのは容量のためではなく、L215 が公式の `Qwen/Qwen3-4B-AWQ` を
第一候補と定めているからである**（Apache License 2.0 であることも L215 に明記されている）。

着手条件は解消したが、代わりに確認すべきことが2つできた。

##### ① sm_120 対応を先に確かめる

RTX 5090 は Blackwell 世代で compute capability が **12.0** である。世代が新しいぶん、
PyTorch・推論ライブラリ・**AWQ の GEMM カーネル**が sm_120 向けのビルドを含むかは版に依存する。
量子化カーネルは世代ごとに手で書かれることが多く、ここが最も外れやすい。

**推論スタックを入れたら、辞書生成の本番より前に最小の生成を1回通す。**

```bash
uv run --group teacher python -c "
import torch; print(torch.__version__, torch.cuda.get_device_capability())"
#    → (12, 0) が出ること。sm_120 を含まないビルドだとここで警告か失敗が出る

#    → 続けて Qwen3-4B-AWQ で1プロンプトだけ生成し、日本語が返ることを確認する
```

**AWQ カーネルが sm_120 で動かない場合は `Qwen/Qwen3-4B`（bf16）に落とす。**
L215 は AWQ 版を「第一候補」としており排他ではない。VRAM は 29GB 空いているので bf16 でも動く。
ただしその場合は L232 の記録項目「量子化方式」を `none` に変え、
**第一候補から外した理由を `data/teacher/manifest.json` と報告書に明記する。**

##### ② GPU は共有されている

確認時点で別プロセスが 3,032 MiB を使用し、GPU使用率は79%だった。空きは 29,080 MiB。

- 教師モデルを回す前に `nvidia-smi` で空きVRAMを見る
- **Phase 3 の学習も同じGPUを使う。**評価指標 L691「最大GPUメモリ（学習時のピークGPUメモリ）」は
  他プロセスと同時に走らせると計測が汚れる。**計測時はGPUを専有する**か、
  専有できなかったことを記録する

#### 依存の層を汚さない

推論スタック（vLLM / transformers / autoawq など）は**巨大な依存**であり、
`boku/` はもちろん `scripts/` の既定にも入れない。

- `pyproject.toml` に `[dependency-groups] teacher` を足し、`uv sync --group teacher` で入れる
- 教師モデルを触るコードは `scripts/teacher/` に閉じる
- `tests/test_core_has_no_external_deps.py` の境界は変更しない
- `tests/test_no_teacher_in_semantics.py`（意味ASTの `source` が全件 `"rule"`）も変更しない

#### 16a：実行環境の構築（辞書生成とは別のチャンクにする）

**2026-08-04 時点で何も入っていない。**`.venv` に torch・transformers・vLLM はいずれも無く、
Qwen の重みも取得していない（HFキャッシュには他プロジェクトのモデルがあるだけ）。

環境構築を辞書生成（16b）と分けるのは、**失敗のしかたが違う**からである。ここで外れるとしたら
sm_120 対応であり、辞書の中身とは無関係である。混ざっていると「AWQカーネルが動かない」のか
「プロンプトが悪い」のかの切り分けに手間がかかる。

##### 推論ライブラリの選択

用途は「24opぶんのプロンプトを1回ずつ、非thinkingモードで流す」だけで、**スループットは要らない。**

| 候補 | 評価 |
| --- | --- |
| **transformers + autoawq** | **第一候補。**依存が軽く、挙動が追いやすい。ただし autoawq が sm_120 のカーネルを持つかは実際に入れて確かめる |
| vLLM | 速いが依存が重く、Blackwell 対応は版による。この用途では速度の利点が効かない |

##### 手順

```bash
# 1. 依存を入れる（teacher グループ）
uv sync --group teacher

# 2. sm_120 のビルドが入っているか
uv run --group teacher python -c "
import torch; print(torch.__version__, torch.cuda.get_device_capability())"
#    → (12, 0) が出ること

# 3. 重みを取得（AWQ版で約3GB）
#    → 回線待ちがあるので先に済ませる

# 4. 疎通確認：1プロンプトだけ生成して日本語が返ること
#    → ここで AWQ カーネルが sm_120 で動くかが判明する
#    → 思考ブロックが出力されていないことも目視で確認する（L240、非thinkingモード）

# 5. 使った版と量子化方式を data/teacher/manifest.json に記録
```

##### AWQ が動かない場合のフォールバック（人間が決める）

**`Qwen/Qwen3-4B`（bf16）に落とす。**L215 は AWQ 版を「第一候補」としており排他ではない。
VRAM は 29GB 空いているので bf16（約8GB）でも動く。その場合は L232 の記録項目
「量子化方式」を `none` に変え、**第一候補から外した理由**を `data/teacher/manifest.json` と
報告書に明記する。

この判断は**辞書生成を始める前に決着させる。**途中で量子化方式が変わると、
同じ辞書の中に由来の違う表現が混ざる。

#### 16b：生成の手順

```bash
# 1. op あたり10〜30種類の日本語表現を生成（L292 第1段）
uv run --group teacher python -m scripts.teacher.generate_phrases \
    teacher.n_per_op=20 teacher.seed=0
#    → data/teacher/raw/phrases_<timestamp>.jsonl（未承認。学習には絶対に使わない）

# 2. 人間が承認（L293 第2段）
uv run python -m scripts.teacher.approve data/teacher/raw/phrases_<timestamp>.jsonl
#    → data/ja/approved_phrases.yaml に承認分だけが入る
```

**非thinkingモードを使う**（L240）。思考過程は保存せず、最終的な表現だけを保存する。
Qwen3 系はチャットテンプレートで thinking を切り替えるので、**実際に思考ブロックが
出力されていないことを目視で確認してから本生成に入る**（版によって指定方法が変わる）。

#### 固定・記録する9項目（L228-238）

生成のたびに `data/teacher/raw/*.jsonl` の各行と、`data/teacher/manifest.json` に記録する。
**由来の正本はこのファイルである**（MLflow は索引。`document_AST.md` §11）。

モデル名 / revision（コミットID）/ 量子化方式（AWQ 4bit。bf16 に落とした場合は `none` と
その理由）/ 推論ライブラリのバージョン / system prompt / sampling設定 / seed / 生成日時 /
**プロンプトのハッシュ値**

あわせて GPU 名と compute capability も残す。sm_120 で動いたかどうかは再現に効く情報である。

レコード側には `teacher_model` `teacher_revision` `prompt_hash` を持たせる（L372-374）。

#### プロンプト設計

op ごとに1プロンプト。**opの意味を日本語で説明した文をこちらから与え、言い換えを求める。**
教師にコードや仕様を推測させない。出力は1行1表現のプレーンテキストで受け、パースの失敗は捨てる。

`multiple_of` のように `k` を参照するopは、スロットを `{k}` のプレースホルダにした形で
生成させる（「{k}で割り切れる値だけを残す」）。日本語側でリテラルに差し替えるのは §2.6 の
カテゴリ3の仕事である。

### 2.8 人間の承認フロー（L293, L314, L323）

**教師モデルの出力を直接学習データにしない。**これが L288-295 の要点である。

`scripts/teacher/approve.py` は1件ずつ提示し、`approve` / `reject` / `edit` を受ける。
承認記録として **承認者・日時・却下理由**を残す。承認済み辞書はリポジトリに追跡する
（人間の判断そのものであり、再生成できないため）。

#### 却下の観点（L323 の例を含む）

| 観点 | 例 |
| --- | --- |
| 境界が動いている | 「以上」が「より大きい」に変わっている（L323 の明示例） |
| 包含関係が変わっている | 「k個」が「k個以下」になっている |
| 対象が変わっている | 「残す」が「取り除く」になっている（否定の反転） |
| 曖昧 | 「小さい順」が昇順か絶対値順か読めない |
| スロットが消えている | `{k}` が失われ具体的な数に置き換わっている |

**difficulty 3以上の指示文は特に注意して承認する**（L314）。ただし辞書は op 単位なので、
difficulty が効くのは第4段（文全体の言い換え、L295）と順序マーカーの連鎖である。
第4段は**通常テスト・組合せ汎化テストの意味ASTに対しては行わない**
（教師の言い換えがテスト側に入ると、訓練と評価の分布差が教師由来のものになる）。

### 2.9 解説文レコード（L325-338、CHANGES.md「残る課題」1）

改訂版は生成手順を定めた上で、**件数とトークン数の目安を「残る課題」としている**（L338）。
本書でこれを決める。

#### 律速は教師モデルではなく人間の承認である

L333-336 は「人間が内容の正しさを確認する」「承認されたものだけを `explanation` フィールドに
保存する」と定める。教師モデルは1万件でも生成できるが、**1万件を人間が読むことはできない。**
したがって件数は承認可能な量から逆算する。

#### 決めた設計

| 項目 | 値 | 根拠 |
| --- | --- | --- |
| 件数 | **300件** | 1件30〜60秒の確認で2.5〜5時間。全件承認が現実的に可能な上限 |
| 1件の長さ | 80〜150 tokens | 初学者向けの短い解説。L432 の指示→コードレコード（65〜145）と同程度 |
| 合計 | **約35K tokens** | 訓練コーパス（35.7M）の 0.1% |
| 抽出単位 | **テーマ**。意味ASTごとではない | 24op ＋ 構造変換7軸 ＋ 基本概念（内包表記、`sorted`、スライス、絶対値…）で約50テーマ。1テーマ6件 |
| 対象 | **訓練集合の意味ASTのみ** | テスト側のASTの解説を学習させると漏洩になる |
| `record_type` | `explanation`、`explanation` フィールドに本文 | L385-388 |

**意味ASTごとに解説を作らない。**45,000件に解説を付けると承認が破綻するうえ、
内容がほぼ同じ文の大量複製になり、L459-460 の「均す」方針にも反する。
テーマ単位にすれば、承認1件あたりの情報量が最大化される。

#### 損失計算への申し送り（Phase 3）

L630-677 のとおり、`record_type` によって損失の対象範囲が変わる。

- `instruction_to_code` → `<|code|>` 以降のトークンのみ（それ以外のラベルを `-100` にする）
- `explanation` → **全トークン**

300件・35K tokens は訓練コーパスの0.15%であり、**日本語の基礎学習として足りるかは実測でしか
分からない。**比較実験（L709-723）の候補として「解説文あり／なし」を残し、
不足なら次のようにスケールさせる。

```
300件（承認2.5〜5時間） → 1,000件（8〜17時間、複数日に分割）→ それ以上は承認方式の見直しが要る
```

### 2.10 データレコード形式（L354-388、schema v2）

`data/dataset/*.jsonl`。L358-381 のフィールドをすべて持つ。

```json
{
  "spec_id": "task-000001",
  "record_type": "instruction_to_code",
  "ast_id": "ast-012345",
  "semantic_ast": ["ge", "double"],
  "instruction_ja": "k以上の値だけを残してから、それぞれを2倍する",
  "reference_code": "def solve(xs, k):\n    ...",
  "explanation": null,
  "tests": [],
  "difficulty": 2,
  "code_style": "...",
  "binding": null,
  "phrase_ids": ["ge-003", "double-007"],
  "split": "train",
  "semantic_hash": "...",
  "text_hash": "...",
  "behavior_hash": "...",
  "teacher_model": null,
  "teacher_revision": null,
  "prompt_hash": null,
  "source": "rule",
  "verification": {"syntax_ok": true, "ast_safe": true, "tests_passed": true},
  "created_at": "...",
  "generator_version": "v0.2",
  "schema_version": 2
}
```

**`schema_version` を 2 に上げる。**`record_type` の追加はフィールドの意味を変えるため
（`document_AST.md` §4 の規定）。`boku/semantics/schema.json` とは別に
`boku/verify/dataset_schema.json` を置き、意味AST層のスキーマは変更しない。

追加した独自フィールドと理由：

| フィールド | 理由 |
| --- | --- |
| `ast_id` | 意味ASTコーパスへの参照。`semantic_ast` の重複保持だけだと突き合わせが文字列比較になる |
| `binding` | `null` or `int`。選抜でリテラル版とパラメータ版の比率を均すのに要る |
| `phrase_ids` | L460「日本語テンプレートを均す」の集計キー。言い換えテストのテンプレート分離（§2.2）の検証にも要る |
| `split` | 漏洩検査の実行を `splits.jsonl` との join に依存させない（検査を独立に回せる） |
| `source` | 意味AST層と同じく `"rule"`。**指示文は教師由来の表現を含むが、レコードの構造はルールベース**である点を、`teacher_model` の有無と分けて記録する |

#### `text_hash` の正規化規則（漏洩検査 第2項の判定単位）

L521 の第2項は `text_hash` の重複を見る。**何を正規化するかで検出力が変わる**ので明示する。

- 対象は `instruction_ja` のみ（コードは第4項が見る）
- 空白の連続を1つに畳み、前後の空白を落とす
- **それ以外は正規化しない。**句読点や表記ゆれを畳むと、意図的に作った言い換えバリエーションまで
  同一視され、言い換えテスト（L506）の設計が壊れる

### 2.11 検証9項目（L443-451）

**全項目を通過したレコードだけを訓練候補にする。**

| # | 検査 | 実装 |
| ---: | --- | --- |
| 1 | `ast.parse` に成功する | 標準ライブラリの `ast`。`boku/semantics/semantic_ast.py` が `ast.py` でないのはこのため |
| 2 | 許可されていない構文を含まない | `ast.NodeVisitor` で許可ノードのホワイトリスト。L178-185 の対象外（クラス・再帰・非同期）を弾く |
| 3 | import・属性アクセス・ファイル操作を含まない | 同上。`Import` / `ImportFrom` / `Attribute` を禁止 |
| 4 | 制限時間内に実行できる | サブプロセス＋タイムアウト。`xs` は最長20（L102）なので本来一瞬で終わる |
| 5 | 参照インタプリタと全テストで一致する | §2.3 の受け入れ条件。**最も重要** |
| 6 | 関数名 `solve`・位置引数 `(xs, k)` | L694-702 の Signature-valid の基準と同じ判定器を使う（Phase 4 と共有する） |
| 7 | 長さ上限を超えない | コンテキスト長256（L399）に収まること。トークナイザ未完成のため**暫定は文字数**で見て、Phase 2 で置き換える |
| 8 | 完全重複コードでない | `reference_code` の完全一致。**同一意味AST内でも、集合全体でも見る** |
| 9 | 同じ意味ASTにデータが偏りすぎていない | 1意味ASTあたり最大20例（L461） |

検査は 1 → 2 → 3 → 4 → 5 の順で行う。**構文と安全性を通してから実行する。**
生成器はルールベースなので危険なコードは出ないはずだが、順序を守るのは
「はずだ」に依存しないためである。

### 2.12 選抜規則（L455-461）と漏洩検査 段2

#### difficulty は均等にしない（L457）

層化抽出の配分をそのまま保つ。**理由を数値ごと引く：** difficulty 1 は意味ASTが24種類しかなく、
1意味ASTあたり最大20例なら480レコード（約5万トークン）が上限である。4水準を均等に揃えると
訓練コーパス全体が約0.2M tokens に制限され、データ規模表の30〜100M tokens に二桁以上届かない。

#### 均すもの（L458-460）

演算子の出現頻度 / コード形式 / 日本語テンプレート。**カテゴリ頻度は均さない**
（`docs/open_questions.md` ⑨。op数の比 filter 10 : map 8 : order 3 : slice 3 から一様にならない）。

集計キーはそれぞれ `semantic_ast` の要素、`code_style`、`phrase_ids`。
**選抜前後の頻度分布を両方 manifest に出す。**均した結果を確認できないと意味がない。

#### 漏洩検査 段2（L521, L523）

分割直後に回した第1・3・5項（`document_SPLIT.md` §2.6）に続けて、展開後にしか回せない2項目を実行する。

| 項 | 検査 | 実装 |
| ---: | --- | --- |
| 2 | 訓練とテストの `text_hash` に重複がない | §2.10 の正規化規則で計算して集合演算 |
| 4 | 訓練コードとテスト正解コードに長い一致がない | 正規化後の**連続40文字以上**の一致、またはトークン列の長い n-gram 一致（L523） |

**第4項は素朴に実装すると O(n²) になる。**訓練34,040種×20例＝最大680,800件と、
テスト943種×20例＝最大18,860件の総当たりは現実的でない。
訓練側の正規化コードから長さ40の**部分文字列のハッシュ集合**（ローリングハッシュ）を作り、
テスト側を走査して照合する。

**第4項は必ず何件か引っかかる。**コード生成器はテンプレートベースなので、
`def solve(xs, k):\n    ys = [x for x in xs if` のような定型部分は訓練とテストで共通する。
**構造の共通と内容の漏洩を区別する必要がある。**定型部分（シグネチャ行、内包表記の骨格）を
正規化で落とすか、閾値を調整するかを実測で決め、**判断とその根拠を報告に残す。**
5項目のうち唯一、機械的な判定で終わらない項目である。

---

## 3. ファイル構成

```
boku/
  expand/binding.py       バインディング列挙・例数配分
  codegen/emit.py         意味AST → Pythonソース（interp を import しない）
  codegen/styles.py       構造変換7軸
  ja/render.py            意味AST → 日本語指示
  ja/phrasebook.py        承認済み表現辞書の読み込み
  verify/checks.py        検証9項目
  verify/select.py        選抜規則
  verify/dataset_schema.json
scripts/
  build_dataset.py        実行の入口（Hydra + MLflow）
  teacher/generate_phrases.py   教師モデル（--group teacher）
  teacher/approve.py            人間の承認フロー
conf/
  expand.yaml             例数・形式数・seed
  teacher.yaml            モデル名・sampling・seed・system prompt
data/
  ja/approved_phrases.yaml      承認済み辞書（追跡する）
  teacher/raw/*.jsonl           教師の生出力（追跡しない。未承認）
  teacher/manifest.json         L228-238 の9項目（追跡する）
  dataset/*.jsonl               最終レコード（追跡しない。再現可能）
  dataset/manifest.json         由来と統計（追跡する）
```

**`data/ja/approved_phrases.yaml` は必ず追跡する。**人間の判断そのものであり、
seed からは再現できない唯一の成果物である。

`boku/` は標準ライブラリのみ。推論スタックは `scripts/teacher/` に閉じる（§2.7）。

---

## 4. テスト

| ファイル | 検査すること |
| --- | --- |
| `test_binding_expansion.py` | `uniform_literal_domain` 外の値を使わない。混在が作られない。`k` 参照なしASTで20例に届く |
| `test_codegen_differential.py` | **差分ランダムテスト**（§2.5）。全 style × 全 binding × ランダム入力 |
| `test_codegen_independence.py` | `boku/codegen/` が `boku/interp/` を import していないこと（AST解析で機械的に） |
| `test_code_styles.py` | 有効な形式が4種類以上。各軸が実際にソースを変えている |
| `test_ja_order_markers.py` | `difficulty >= 2` の指示文に順序マーカーが必ず入る。3以上で連鎖する |
| `test_ja_no_duplicate_instruction.py` | 異なる意味ASTから同一の指示文が出ない |
| `test_phrasebook_split.py` | 訓練用と言い換えテスト用の `phrase_id` が交わらない |
| `test_verify_checks.py` | 9項目。**故意に壊したコードで落ちること**を確かめる |
| `test_leakage_stage2.py` | 第2・4項。故意に漏洩させた入力で検出できること |
| `test_dataset_schema.py` | schema v2 の往復。`record_type` による `explanation` の要否 |
| `test_no_teacher_in_code.py` | `reference_code` が教師由来でないこと（`teacher_model` が付くのは `instruction_ja` と `explanation` だけ） |

**「落ちること」を必ず確かめる。**前半で偽の衝突30件を見つけたのはこの姿勢である
（`HANDOFF.md` §7）。

---

## 5. 検証手順

```powershell
# 1. 全テスト
uv run pytest -q

# 2. 教師モデルの実行環境（16a、§2.7）
nvidia-smi                                    # 空きVRAMを確認（GPUは共有）
uv sync --group teacher
uv run --group teacher python -c "import torch; print(torch.cuda.get_device_capability())"
#    → (12, 0)。sm_120 のビルドが入っていること
#    → 続けて1プロンプトだけ生成し、AWQカーネルが動くことを確認する
#    → 動かなければ bf16 の Qwen/Qwen3-4B に落とすかを人間が決める（16b の前に決着させる）

# 3. 日本語表現の生成と承認（16b、人間が入る）
uv run --group teacher python -m scripts.teacher.generate_phrases teacher.n_per_op=20
uv run python -m scripts.teacher.approve data/teacher/raw/phrases_<ts>.jsonl
#    → op あたり承認済み15表現以上（訓練用12・言い換えテスト用3を確保）

# 4. データセット構築
uv run python -m scripts.build_dataset
#    → 訓練 34,040種 × 最大20例、検証9項目の通過率を報告
#    → 通過率が 50% を下回る場合はデータ規模の帯に届くか再検算する（§1）

# 5. 漏洩検査 段2
#    → 第2項（text_hash）は0件であること
#    → 第4項は件数と、定型部分をどう扱ったかの判断を報告

# 6. 選抜前後の頻度分布
#    → op頻度・code_style・phrase_ids が均されていること

# 7. 再現性
uv run python -m scripts.build_dataset
#    → manifest が created_at 以外一致（承認済み辞書が同じであること）
```

手順4の通過率が最初の関門。**§1 の帯の問題がここで数値として出る。**

---

## 6. `docs/open_questions.md` に追記する事項

| # | 事項 | 種別 |
| ---: | --- | --- |
| 17 | **L424-437 の検算が L465 の分割順序と噛み合っていない**（§1）。全数に20例を掛けているが、展開されるのは訓練集合だけ。`target` を 45,000 に上げて対処し、difficulty 4 の比率が 57.6% → 71.7% に上がることを受け入れた | 仕様の食い違い（2026-08-04 決着） |
| 18 | 解説文レコードは300件・テーマ単位（§2.9）。律速は教師モデルではなく人間の承認 | 実装上の解釈（残る課題1への回答） |
| 19 | 教師の言い換え（L295 第4段）はテスト側の意味ASTに適用しない | 実装上の解釈 |
| 20 | 漏洩検査第4項は定型部分で必ず一致が出る。構造の共通と内容の漏洩の区別は実測で決める（§2.12） | 実装上の解釈 |
| 21 | 検証9項目の第7項（長さ上限）は、トークナイザ完成まで文字数で代用する | 暫定。Phase 2 で置き換える |

---

## 7. Phase 2（トークナイザ）への申し送り

- **出力の絶対値は最大7桁**（`data/ast/manifest.json`）。`mul_k`@10 → `square` で1,000,000に達する。
  語彙4,096（L544）で数値をどう分割するかに直結する
- **リテラルは1〜10の10種類しかない。**コード中の数値リテラルの語彙は極めて狭い
- トークナイザは**訓練データだけ**で学習する（L540）。`splits.jsonl` の `train` に限ること
- 特殊トークンは `<|pad|> <|bos|> <|eos|> <|task|> <|code|> <|explanation|>`（L552-557）。
  `record_type` と対応する（§2.9）

---

## 8. `document_AST.md` §10 のフックの回収先

前半で「今回スコープ外だが余地を残しておくもの」として残した10項目が、後半のどこで回収されるかの対応。

| 前半で残したフック | 回収先 |
| --- | --- |
| バインディング生成器（展開層） | 本書 §2.1 |
| コード→ASTパーサ | **回収しない。**`docs/canonical_forms.md` に置いたまま Phase 5 以降へ |
| コード生成器・日本語生成器の Protocol、`ja_key` | 本書 §2.3〜§2.6 |
| train/val/test 分割 | `document_SPLIT.md` §2.1〜§2.5 |
| 漏洩検査5項目 | `document_SPLIT.md` §2.6（第1・3・5項）＋ 本書 §2.12（第2・4項） |
| hidden test 生成器 | `document_SPLIT.md` §2.8（インタフェースのみ。実装は Phase 4） |
| 解説文レコード（`record_type`） | 本書 §2.9、§2.10 |
| トークナイザ（出力桁数・リテラル範囲） | 本書 §7 |
| 比較実験（`MAX_OPS` / `target` / `drop_behavior_duplicates`） | `document_SPLIT.md` §2.2（予約ペア数 N）＋ 本書 §2.9（解説文あり／なし）＋ §1（`target`） |
| **順序多様性のアブレーション（`canonical_order`）** | **本書では設計しない。**Phase 4 の比較実験の候補として残す。レコードは `ast_id` で意味ASTコーパスを引けるので、`canonical_order` による絞り込みは後から可能である（レコードにフィールドを複製する必要はない） |
