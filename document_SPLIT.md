# 実装計画：Boku-nano Phase 1 後半(1)（訓練・検証・テスト分割 ＋ 漏洩検査）

## Context

Phase 1 前半（意味AST層 ＋ 参照インタプリタ）が完了し、意味ASTコーパスが 45,000件ある。

```
uv run pytest -q                              537 passed
uv run python -m scripts.build_ast_corpus     45,000件（conf の既定 target=45,000）
```

**`target` は 30,000 から 45,000 に上げた**（2026-08-04）。理由は `document_EXPAND.md` §1。
分割してから展開する以上、展開されるのは訓練集合だけであり、30,000 ではデータ規模表の帯に届かない。
本書の実測値はすべて 45,000 のコーパスに対するものである。

本書は、そのコーパスを **訓練 / 検証 / 通常テスト / 組合せ汎化テスト** に切り分ける工程の実装計画である。

**この工程を展開（日本語指示とコードの生成）より先に置く。** 改訂版 L465 が
「データは日本語文やコードを生成する前に、意味AST単位で分割する」と定めており、
順序を逆にすると同じ問題の表記違いが訓練とテストの両方に入るためである。
展開層の実装計画は `document_EXPAND.md`（本書の次）に分けた。

### 規範とする文書

`CLAUDE.md` の参照順序に従う。**推測で埋めない。**

1. `芦原・倉林研究室_夏休み実習課題_Boku-nanoの開発_改訂版.md` — 唯一の正。本書の行番号（L…）はすべてこれ
2. `document_AST.md` — Phase 1 前半の実装計画。§3（固定入力集合と hidden test の分離）と §4（レコード形式）を前提にする
3. `docs/open_questions.md` — 課題文の解釈と根拠。⑥⑦⑧⑫が本工程に効く

改訂版の記述を本書やコードが上書きしてはいけない。食い違いを見つけたら寄せずに報告する。

### 今回作るもの

| モジュール | 役割 |
| --- | --- |
| `boku/split/reserve.py` | 予約ペアの選定と引き当て（手順1・2） |
| `boku/split/partition.py` | 層別 80/10/10 分割（手順3） |
| `boku/split/leakage.py` | 漏洩検査5項目のうち、分割直後に回せる第1・3・5項 |
| `boku/split/manifest.py` | `splits.jsonl` と `split_manifest.json` の読み書き |
| `scripts/build_splits.py` | 実行の入口（Hydra ＋ MLflow を触るのはここだけ） |
| `conf/split.yaml` | 設定 |

### 今回作らないもの

| 作らないもの | 置き場所 |
| --- | --- |
| 日本語指示・コードの生成、バインディング展開 | `document_EXPAND.md` |
| 漏洩検査 第2項（`text_hash`）・第4項（長い文字列一致） | 同上。**展開後にしか回せない**（§2.6） |
| hidden test の入力生成器 | Phase 4（評価）。本書では**インタフェースだけ決める**（§2.8） |
| 言い換えテスト・境界値テストの中身 | それぞれ `document_EXPAND.md` と Phase 4。本書では集合の定義だけ置く |

---

## 1. なぜ分割が最初に来るのか（L465）

意味ASTは操作列だけを表し、定数を含まない（L256）。1つの意味ASTから、バインディング×日本語表現×
コード形式で最大20例が展開される（L428）。展開してから分割すると、**同じ意味ASTから出た兄弟レコードが
訓練とテストに分かれる。**モデルにとっては答えを見てから解くのと同じであり、通常テストの数値が
実際より高く出る。

したがって順序は次で固定する。逆順を許さない。

```
意味ASTコーパス 45,000件
   ↓ 手順1・2   予約ペアを含むASTを抜く         → 組合せ汎化テスト
   ↓ 手順3      残りを意味AST単位で 80/10/10    → 訓練 / 検証 / 通常テスト
   ↓ 漏洩検査 段1（第1・3・5項）                 ← ここまでが本書
   ↓ 手順4      各集合を独立に展開               ← document_EXPAND.md
   ↓ 漏洩検査 段2（第2・4項）
```

---

## 2. 確定した設計判断

### 2.1 分割の単位は意味AST（`semantic_hash`）

L465 が「意味AST単位で分割する」と定めている。`semantic_hash` はop列だけから決まるので
（`document_AST.md` §4）、分割の一次キーにそのまま使える。

`ast_id` を使わない。採番順は列挙器の都合であり、意味を持たないため。

### 2.2 手順1：予約ペアは順序非依存・異カテゴリ・既定 N=3（L469-496）

**引き当てキーは `op_set`（順序を落としてソート済み）。** L472 の予約で L485 の
「個々の操作は学習したが、その組合せは見ていない」状況を作るには、`[even, desc]` と
`[desc, even]` の**両方**を除外しなければならない。片方だけ残すと、組合せは訓練で見えてしまう
（`docs/open_questions.md` ⑧）。L487 の割合が `C(22,2)/C(24,4)` と**組合せ**で書かれていることとも整合する。

L496 は「異なるカテゴリの組合せ（抽出×並べ替え、変換×切り出しなど）から選び、汎化の種類が
偏らないようにする」と定める。既定の3組はカテゴリの組を散らして選ぶ。

| 予約ペア | カテゴリの組 | 除外される意味AST |
| --- | --- | ---: |
| `(even, desc)` | 抽出 × 並べ替え | 810 |
| `(abs, take_first)` | 変換 × 切り出し | 840 |
| `(gt, square)` | 抽出 × 変換 | 805 |
| **合計（N=3）** | | **2,450（全体の 5.4%）** |

**割合で見ると L490 の「N=3 で約1,500種類（全体の約5%）」と一致する**（実測 5.4%）。
件数が予測と違うのは L487-493 の数値が 30,000件の配分例を前提にしているためで、`target` に比例して増える。
ペア間の重なりは0件（3組では同一ASTが複数ペアに該当しない）。

1ペアあたりの比率は全体の 1.8%（30,000件のときは 1.66%）。L487 の「約2.2%」に近づくのは、
`target` を上げた分がすべて difficulty 4 に乗ったためである（L487 の 2.2% は4操作を前提にした値）。

N=5 まで増やした場合の実測は 4,131件（9.2%）で、L491 の「約8%」より1ポイント高い。
**既定は N=3。** L494 が示すとおり N=1〜2 では選んだペアの性質に結果が左右され、
N=10 では除外が約16%に達して通常テストを上回る。N は `conf/split.yaml` で変えられるようにし、
L816 の比較実験に使えるようにする。

**difficulty 1 は予約ペアで一切除外されない。**操作が1個ならペアを含み得ないためで、
実測でも difficulty 別の内訳は 2:6件 / 3:396件 / 4:2,048件 となり difficulty 1 は0件である。
これは仕様どおりの帰結であり、L477「単体操作 (even単体, desc単体) は訓練側に残す」を自動的に満たす。

### 2.3 手順2：組合せ汎化テスト集合（L475-483）

予約ペアを含む意味ASTを**訓練からも検証からも**外し、まとめて組合せ汎化テストにする。

**単体操作は訓練側に残す**（L477 の※、L485）。`even` 単体・`desc` 単体を含むASTは、予約ペアの
**両方**を同時に含まない限り訓練に残る。この性質は漏洩検査第3項（L522）が機械的に検査する。

### 2.4 手順3：残りを difficulty 層ごとに 80 / 10 / 10（L479）

**全体を一括でシャッフルせず、difficulty ごとに分ける。** L457 が「操作数（`difficulty`）の分布は、
層化抽出の配分をそのまま保つ。難易度を均等にはしない」と定めており、一括分割では層の比率が
乱数で揺れるためである。層別に分ければ比率は設計どおりに保たれる。

実測（N=3、seed 固定）：

| difficulty | 分割対象 | 訓練 | 検証 | 通常テスト |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 24 | 19 | 2 | **3** |
| 2 | 546 | 437 | 55 | 54 |
| 3 | 11,748 | 9,398 | 1,175 | 1,175 |
| 4 | 30,232 | 24,186 | 3,023 | 3,023 |
| 合計 | 42,550 | 34,040 | 4,255 | 4,255 |

**difficulty 1 の通常テストは3件しかない。**意味ASTが24種類しかない（L403-411）ことの直接の帰結で、
10%を取れば2〜3件にしかならない。これは仕様の帰結であって不具合ではないが、
**difficulty 1 の通常テストの数値はノイズとして読むこと。**§2.6 の漏洩検査を通すとさらに1件まで減る。
`target` を上げてもこの数は増えない（difficulty 1 は24種類で頭打ちのため）。
評価時に difficulty 別の pass@1 を出すなら、この件数を必ず併記する。

乱数の名前空間：分割の seed は固定入力集合（`document_AST.md` §3）や hidden test とは別に持つ。
`split_manifest.json` に記録し、difficulty ごとに派生させる（コーパス構築と同じ方式）。

### 2.5 手順4への受け渡し（L482-483）

分割結果は `data/split/splits.jsonl` と `data/split/split_manifest.json` に出す（§3）。
展開層はこのファイルだけを入力に取り、**どのASTがどの集合に属するかを再計算しない。**
再計算を許すと、展開層の乱数や実装の変更で分割が動き、漏洩の有無が再現できなくなる。

### 2.6 漏洩検査5項目は2段階に分けて実行する（L516-536）

改訂版は5項目を並べているが、**回せる時点が違う。**

| 項 | 検査内容 | 実行できる時点 | 実装場所 |
| ---: | --- | --- | --- |
| 1 | 訓練とテストの `semantic_hash` に重複がない（L520） | 分割直後 | `boku/split/leakage.py` |
| 2 | 訓練とテストの `text_hash` に重複がない（L521） | **展開後** | `document_EXPAND.md` |
| 3 | 予約ペアが訓練集合の意味ASTに1つも出現しない（L522） | 分割直後 | `boku/split/leakage.py` |
| 4 | 訓練コードとテスト正解コードに長い一致がない（L523） | **展開後** | `document_EXPAND.md` |
| 5 | 訓練とテストの `behavior_hash` に重複がない（L524） | 分割直後 | `boku/split/leakage.py` |

第2項と第4項は日本語文とコードが存在しないと判定できない。段1で第1・3・5項を通し、
段2で第2・4項を通す。**報告は5項目まとめて出す**（L518「結果を報告する」、ゴール条件 L737）。

#### 第1項と第3項は構成上ほぼ自明である

分割単位が `semantic_hash` なので第1項は定義上通る。第3項も手順2の直接の帰結である。
**それでも検査として実装する。**分割器のバグ（層別分割の端数処理、`op_set` の作り方の誤り）は
静かに混入するので、機械的なガードを常設する。

#### 第5項が本工程で最も重いもの

L536 は「重複が見つかった場合は、テスト側のレコードを除外する」と定める。
意味ASTコーパスの実測では `behavior_hash` の衝突に **80.8%**（36,339件）が関与しており
（`data/ast/manifest.json`）、第5項を適用するとテスト集合が大きく削れる。

実測（N=3、層別分割後）：

| 基準集合 | 通常テスト | 組合せ汎化テスト |
| --- | ---: | ---: |
| 分割直後 | 4,255 | 2,450 |
| **訓練のみと照合（採用）** | **943（22.2%）** | **1,425（58.2%）** |
| さらにテスト内部の重複も1件に畳む | 909 | — |
| （参考）訓練＋検証と照合 | 873（20.5%） | 1,396（57.0%） |

**通常テストの約78%が消える。**それでもゴール条件 L734「未学習の意味ASTに対して100問以上
評価している」は満たす（943件、9倍の余裕）。組合せ汎化テストは6割弱が残る。

`target` を 30,000 から 45,000 に上げたことで、通常テストは 682件 → 943件 に増えた。
削られる割合はどちらも約78%で変わらず、絶対数だけが増えている。

**基準集合は訓練集合のみ。L524 の字義どおりにする。**

一度は検証集合も含める（＝より厳しい）案を採ったが、2026-08-04 に撤回した。理由は3つある。

1. **検証集合は勾配更新に使われない。**テストASTが検証ASTと behavior を共有していても、
   モデルはその問題を一度も学習していない。L524 が塞ぐのは train↔test の経路であり、
   検証集合の混入は漏洩そのものではなく、チェックポイント選択という二次的な効果にすぎない
2. **その二次的な効果も効きにくい。**選択は検証集合4,255件の集計指標で行うので、
   特定の1問の behavior が選抜を左右する余地は小さい。比較実験も L711 の「最低1つ、可能なら2つ」で、
   検証集合の癖に選抜がすり寄るほどの反復探索はしない
3. **代償が割に合わない。**通常テストで70件、組合せ汎化で29件を失う

**さらに §8 の約束（固定ステップ数で学習し、採用ルールを事前宣言する）によって、
2 の経路そのものが閉じる。**検証成績でチェックポイントを選ばないなら、
検証集合とテスト集合の behavior 重複は何にも影響しない。

**ただし報告すべき統計は残る。**残った通常テスト943件のうち **70件は検証集合と behavior が一致する。**
また検証集合4,255件のうち **3,328件（78.2%）は訓練集合と behavior が一致する。**
後者は漏洩ではないが（検証は評価対象ではない）、**検証lossと検証pass率が楽観的に出ることを意味する。**
ゴール条件 L731「訓練lossと検証lossの推移を提示できる」で曲線を読むとき、この数値を併記すること。

**検証集合から重複を除くことはしない。**L524 が求めていないうえ、選抜に使わない以上、
除いて得られるものがない。ただし訓練と behavior が重ならない **927件（clean val）** を
部分集合として識別できるようにしておく。曲線を「暗記を含む全体」と「含まない部分」に
分けて描けると、学習の中身の説明がしやすくなる。clean val は difficulty 4 が 90.7% を占め、
**通常テスト（89.2%）とほぼ同じ分布になる**——両方に同じフィルタをかけた結果である。

**テスト集合の内部重複を畳むかは選抜段の判断にする。**L536 は訓練との重複だけを問題にしており、
テスト内部に同一 behavior のASTが複数あっても漏洩ではない（評価が同じ問題を重複して数えるだけ）。
既定では畳まず、件数だけ報告する（909 と 943 の差＝34件）。

#### 副次効果：`always_empty` のASTはテストから自動的に消える

`always_empty`（常に `[]` を返す）は 8,903件あり、最大の衝突グループを成す
（`data/ast/manifest.json`）。この群は必ず訓練側にも存在するため、第5項によって
**テスト側からは1件残らず除外される**（実測：漏洩検査後の通常テスト 943件中 `always_empty` は0件）。

`docs/open_questions.md` ⑦「`always_empty` の扱いを選抜段で決めること」への回答の一部がこれである。
訓練データに残る 19.8% をどうするかは `document_EXPAND.md` の選抜規則で別途決める。

#### difficulty 4 の偽の衝突は過剰除外として現れる

固定入力集合の受け入れ条件は difficulty 1〜3 でしか検証されていない
（`docs/open_questions.md` ⑫）。difficulty 4 に残る偽の衝突は、
**漏洩する方向ではなく、重複でないテストASTが理由なく除外される方向**に働く。
第5項で除外した件数を difficulty 別に報告し、difficulty 4 の除外率が 1〜3 に比べて
不自然に高くないかを確認する。

### 2.7 テスト集合4種の定義（L498-514）

| 集合 | 定義 | 決まる時点 |
| --- | --- | --- |
| **通常テスト**（L502） | 手順3でテスト側になった意味AST。日本語テンプレートと `k` の値も訓練と重複させない | 集合は本書、テンプレートと `k` は展開層 |
| **言い換えテスト**（L506） | 訓練に使わなかった日本語テンプレートを使う | `document_EXPAND.md` |
| **組合せ汎化テスト**（L510） | 手順1で予約した組合せ | 本書 |
| **境界値テスト**（L514） | 空リスト、要素1個、すべて不合格、すべて同値、負数を含むリスト | 入力側の話。Phase 4 の hidden test 生成器 |

**境界値テストは意味ASTの集合ではなく入力の集合である。**他の3つと軸が違うので、
`splits.jsonl` には現れない。混同しないこと。

L502 の「`k` の値も訓練と重複しないものを用いる」は展開層への制約になる。
`k` は1〜10しかないので（L104）、全レコードで重複させない運用は不可能である。
**同一意味ASTの中で訓練とテストの `k` を重複させない**と読む。ただし通常テストの意味ASTは
そもそも訓練に出現しないため、この制約が効くのは言い換えテストだけである。
解釈として `docs/open_questions.md` に記録し、`document_EXPAND.md` で実装する。

### 2.8 hidden test 生成器のインタフェースだけ決める（L707）

L707 は「テスト問題ごとに20個以上のランダム入力を生成する。コード作成時に使用したテストとは
異なる hidden test で判定する」と定める。生成器の実装は Phase 4 だが、
`document_AST.md` §3 が用意したフックを本書で確定させる。

- `boku/probes/behavior_probes.py` の `load_probe_inputs() -> set[tuple[tuple[int, ...], int]]` を読み、
  **衝突する `(xs, k)` を除外する**
- 乱数の名前空間を分割・固定入力集合のいずれとも分ける
- 入力は `boku/limits.py` の条件（`xs` 長さ0〜20、要素 ±100、`k` 1〜10。L102-105）を守る

---

## 3. 出力するファイル

### `data/split/splits.jsonl`

意味ASTごとに1行。**コーパスのレコードを複製せず、割り当てだけを持つ。**
コーパス側が正本であり、二重に持つと更新時に食い違うため。

```json
{
  "ast_id": "ast-000001",
  "semantic_hash": "...",
  "split": "train",
  "difficulty": 1,
  "excluded_by": null,
  "corpus_manifest_hash": "..."
}
```

| フィールド | 用途 |
| --- | --- |
| `split` | `train` / `val` / `test` / `comb_test` のいずれか |
| `excluded_by` | 漏洩検査で除外された場合の理由（`behavior_dup` など）。除外レコードも**行として残す**。消すと除外の内訳を後から検証できない |
| `corpus_manifest_hash` | どのコーパスに対する分割かを固定する。コーパスを作り直したら分割も無効になる |

### `data/split/split_manifest.json`

L496 が「実際に予約したペアと、除外された意味ASTの件数を報告すること」と求めているので、
これが報告の正本になる。MLflow は索引であって正本ではない（`document_AST.md` §11）。

記録する内容：seed、予約ペアの一覧とペアごとの除外件数、N、集合ごとの件数（difficulty 別も）、
漏洩検査5項目のうち段1の結果、第5項の除外件数（difficulty 別・基準集合の別）、
`always_empty` の除外内訳、対応するコーパスの `manifest.json` のハッシュ、各 version。

`.gitignore` は `splits.jsonl` を追跡せず（seed から再現できる）、
`split_manifest.json` は追跡する。コーパスと同じ方針である。

---

## 4. ファイル構成と依存の層

```
boku/split/
  reserve.py     予約ペアの選定と引き当て（op_set ベース）
  partition.py   層別 80/10/10
  leakage.py     漏洩検査 第1・3・5項
  manifest.py    splits.jsonl / split_manifest.json
scripts/build_splits.py
conf/split.yaml
data/split/      splits.jsonl（追跡しない） split_manifest.json（追跡する）
```

`boku/split/` は**標準ライブラリのみ**。Hydra と MLflow を触るのは `scripts/` だけ
（`document_AST.md` §11）。この境界は `tests/test_core_has_no_external_deps.py` が機械的に守るので、
新しいパッケージを足したらそのテストの対象リストに加える。

`conf/split.yaml` は `hydra.job.chdir: false` と `output_subdir: null` を必ず入れる
（相対パスが `outputs/日付/時刻/` に逃げる。`document_AST.md` §11 の注意2）。

---

## 5. テスト

| ファイル | 検査すること |
| --- | --- |
| `test_reserve_pairs.py` | 予約が**順序非依存**（`[even,desc]` と `[desc,even]` の両方が抜ける）。単体opのASTは残る。異カテゴリ制約 |
| `test_partition_strata.py` | difficulty 別の 80/10/10 が保たれる。端数処理が層をまたいで偏らない |
| `test_split_reproducible.py` | 同じ seed で同じ分割。`created_at` 以外が完全一致 |
| `test_leakage_checks.py` | 第1・3・5項。**故意に漏洩させた入力で検査が落ちること**を確かめる（通ることだけ見ても検査になっていない） |
| `test_split_manifest_roundtrip.py` | `splits.jsonl` / `split_manifest.json` の往復 |
| `test_core_has_no_external_deps.py`（既存に追記） | `boku/split/` が標準ライブラリのみ |

**漏洩検査のテストは「落ちること」を必ず確かめる。** 前半で「テストが通っても意味が正しいとは
限らない」（`HANDOFF.md` §7）に当たったのと同じ穴なので、反例を先に作る。

---

## 6. 検証手順

```powershell
# 1. 全テスト
uv run pytest -q

# 2. 既定の分割（N=3）
uv run python -m scripts.build_splits
#    → 組合せ汎化 2,450件（5.4%）、訓練 34,040 / 検証 4,255 / 通常テスト 4,255
#    → 漏洩検査後の通常テスト 943件、組合せ汎化 1,425件（基準は訓練集合のみ）
#    → difficulty 1 の通常テストが 3件 → 漏洩検査後 1件 であること

# 3. ペアごとの除外件数が全体の 1.8% 前後に収まること
#    → 実測 805〜866（L487 の「約510種類」は 30,000件前提の値なので件数では比較しない）

# 4. N=5（L816 の比較実験の条件）
uv run python -m scripts.build_splits reserve.n=5
#    → 組合せ汎化 4,131件（9.2%）

# 5. 再現性
uv run python -m scripts.build_splits
#    → split_manifest.json が created_at 以外完全一致

# 6. 漏洩検査が落ちること
#    → テストASTを1件訓練に混ぜ、第1項が検出することを確認
```

手順2の数値が最初の関門。ここがずれたら層別分割か `op_set` の引き当てを疑う。

---

## 7. `docs/open_questions.md` に追記する事項

課題文に明記がなく、本工程で決めた解釈。**報告書の材料になる**ので根拠付きで残す。

| # | 解釈 | 根拠 |
| ---: | --- | --- |
| 13 | 漏洩検査第5項の基準集合は**訓練集合のみ**（L524 の字義どおり） | 一度は検証集合も含める案を採ったが 2026-08-04 に撤回した。検証集合は勾配更新に使われないので、検証と behavior を共有するテストASTもモデルは未学習である。残るのはチェックポイント選択という二次効果だけで、それも集計指標での選択なら効きにくい。代償（通常テスト70件・組合せ汎化29件）に見合わない。**撤回の経緯ごと記録する**（判断を変えたこと自体が報告書の材料になる） |
| 13b | 検証集合の `behavior_hash` 重複は**除かず、統計として報告する** | 検証集合4,255件のうち3,328件（78.2%）が訓練集合と behavior 一致。漏洩ではないが検証lossと検証pass率が楽観的に出る。L731 の曲線を読むときに併記する。§8 の固定ステップ数によりチェックポイント選択には使わないので、除いて得られるものがない。重ならない927件（clean val）は部分集合として識別できるようにしておく |
| 13c | **チェックポイントの採用ルールを事前に宣言する**（§8） | テスト成績を見てから選ぶと、報告する pass@1 が「複数の中の最良」になる。argmax を計算するのが人間でも同じ。漏洩検査5項目はデータの重複しか見ないので、この手続き側の漏洩を検出できない |
| 14 | テスト集合**内部**の `behavior_hash` 重複は畳まない | L536 が問題にしているのは訓練との重複。内部重複は漏洩ではない。件数のみ報告（実測 34件） |
| 15 | L502「`k` の値も訓練と重複しない」は**同一意味AST内**の制約と読む | `k` は1〜10しかなく（L104）、全体で重複させない運用は不可能。通常テストの意味ASTは訓練に出現しないため、実効的に効くのは言い換えテストのみ |
| 16 | 80/10/10 は difficulty 層ごとに行う | L457 の層化配分の保持。一括分割では比率が乱数で揺れる |

あわせて `docs/open_questions.md`「分割段への申し送り」に実測の回答を書き戻す
（第5項で通常テストが 4,255 → 943 に削れるが、L734 の100問は満たす）。

---

## 8. 次の工程（`document_EXPAND.md`）への申し送り

1. **展開は `splits.jsonl` を入力に取り、分割を再計算しない**（§2.5）
2. **漏洩検査 第2項（`text_hash`）と第4項（長い文字列一致）は展開層の担当**（§2.6）。
   `text_hash` の正規化規則を決めるのは展開層である
3. **通常テストは943件しかない。**言い換えテストと組合せ汎化テストの設計で、
   評価に足る問題数を確保すること（L734 は100問以上）
4. **difficulty 1 の通常テストは1件。**difficulty 別の評価を出すなら件数を併記する
5. **`always_empty` はテストから自動的に消えるが、訓練には 19.8% 残る。**
   選抜規則で扱いを決めること（`docs/open_questions.md` ⑦）
6. **訓練集合は34,040種。**`target=45,000` の根拠（`document_EXPAND.md` §1）はこの数に依存する。
   予約ペア数 N を増やすと訓練が削れ、データ規模の帯に効く

---

## 9. テスト集合の使い方の約束（Phase 3・4 への申し送り）

**この節はまだ実装していない工程（学習ループと評価）に対する取り決めである。**本書の範囲では
テスト集合を作るところまでしかやらない。ここに書くのは、せっかく漏洩検査で守った集合を
**使い方の側から壊さない**ための約束である（2026-08-04 決定）。

### 9.1 固定ステップ数で学習し、採用ルールを事前に宣言する

改訂版 L626 は総step数を 3,000〜9,000、L627 は checkpoint を 500 step ごとと定めている。

| 項目 | 決めたこと |
| --- | --- |
| 学習ステップ数 | `conf` に固定値で宣言する。比較実験（L711）では全条件で共通にする |
| チェックポイント | 500 step ごとに保存（L627）。総6〜18個になる |
| 各チェックポイントで測るもの | 訓練loss / 検証loss（L731 の曲線用）/ clean val の指標 / **通常テストの pass@1** → すべて MLflow に記録 |
| **採用するチェックポイント** | **`conf` に事前宣言する。既定は最終チェックポイント** |
| ルールを覆した場合 | MLflow の run tag と報告書に「テスト成績を見て選んだ」と明記し、その pass@1 は上振れした推定値として扱う |

**テスト成績を全チェックポイントぶん MLflow に残すこと自体に害はない。**「テスト pass@1 が
学習ステップとともにどう伸びたか」という曲線は、ゴール条件 L730「学習前後の出力を比較できる」の
良い材料になる。害が出るのは、その記録を**選択肢の一覧として使ったとき**だけである。

**覆すことを禁止はしない。記録すればよい。**禁止すると、実際には見て決めたのに記録だけ
「最終チェックポイント」になる、という一番まずい形になりやすい。読む側が数値の性質を
判断できることの方が大事である。

**固定ステップ数にすると §2.6 の懸念が消える。**検証成績で何かを選ばないなら、
検証集合とテスト集合の behavior 重複は影響しない。検証loss は L731 のために出すが、
それで選抜はしない。

### 9.2 テスト評価は943問すべてで回す（部分集合を作らない）

一度は「曲線用に部分集合、最終評価だけ全問」という案を出したが、**実測の見積りで不要と分かった**
ため撤回した（2026-08-04）。部分集合にすると曲線と最終値が食い違い、説明が増えるだけ損である。

Boku-nano は 13,965,696 パラメータ・最大系列長256（L560-585）。この規模では**モデルの計算は
支配項にならない。**

```
生成の計算量   943問 × 約60トークン = 56,580 トークン
               2 × 13,965,696 × 56,580 = 1.58 TFLOPs   → RTX 5090 で1秒未満
```

実際に効くのは次の2つで、**どちらも実装の書き方で決まる。**

| 要因 | 素朴に書いた場合 | 実装で守ること |
| --- | ---: | --- |
| **GPU呼び出しの回数** | 1問ずつ生成すると 56,580回 ≒ 57秒 | **943問をバッチにする。**呼び出しは最長出力トークン数ぶん（約60回）で済む |
| **hidden test の実行** | 入力ごとに subprocess を起こすと 18,860回 × 20ms ≒ 377秒 | **検証9項目の1〜3 を通した後は同一プロセスで実行する。**タイムアウトはシグナルかスレッドで見る |

貪欲デコードは1トークンずつ進むので、1問ずつ処理すると「トークン数 × 問題数」回だけGPUを呼ぶ。
ここがバッチ化の効く理由である。

hidden test 側は、`ast.parse`・禁止構文・import と属性アクセスの排除
（`document_EXPAND.md` §2.11 の項目1〜3）を**先に**通してあれば同一プロセスで実行してよい。
`xs` は最長20要素（L102）なので関数1回の実行はマイクロ秒である。
§2.11 で「検査は 1→2→3→4→5 の順で行う」と決めたのは、この実行を安全にするためでもある。

**この2点を守れば、943問フルの評価は1チェックポイントあたり数秒**で、
全チェックポイント（6〜18個）を回しても1分に満たない。

**GPU は他プロセスと共有している**（`document_EXPAND.md` §2.7）。評価は軽いので影響は小さいが、
評価指標 L691「最大GPUメモリ」を計測するときだけはGPUを専有すること。
