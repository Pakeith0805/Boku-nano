"""`behavior_hash` 用の固定入力集合（実装計画 §2.5, §3）。

**評価用の hidden test とは目的が違うので混ぜない**（実装計画 §3）。

| | 固定入力集合（この層） | hidden test（将来 `boku/evalgen/`） |
| --- | --- | --- |
| 目的 | 意味の同定 | 評価 |
| 設計方針 | 識別力を最優先。現実的な分布は考えない | 現実的な入力分布 |
| 出典 | 改訂版 L536 | 改訂版 L707 |
"""
