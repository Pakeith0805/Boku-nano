"""日本語指示生成器のインタフェース宣言（実装計画 §1「今回作らないもの」、§5, §10）。

**宣言のみ。実装しない。**

## 教師モデルの出力を直接学習データにしない

改訂版 L288-295 の四段階を守る。要点は**必ず人間の承認を挟む**ことである。

1. 教師モデルにそれぞれのアトミックな操作について10〜30種類の日本語表現を生成させる
2. 人間が、生成させたものに意味の変化がないか確認し、**承認済み表現辞書**を作る
3. **ルールベースの文生成器**が、意味ASTと承認済み表現を組み合わせる ← この層
4. 一部についてのみ、教師モデルに文全体の言い換えを行わせる

教師が生成した文には「以上」が「より大きい」に変わるなど意味を変える言い換えがあるため、
そのまま無条件に採用してはいけない（L323）。

## 承認済み表現辞書は3カテゴリを別に持つ

`OpSpec.ja_key` が1番目の引き当てキーになる（実装計画 §5）。

1. **op別の表現**（L316-321 の例：「偶数の要素だけを抽出する」「2で割り切れる値のみを残す」…）
2. **順序マーカー**（「〜してから〜」「〜した後で〜」「まず〜、次に〜」）と、その連鎖規則
3. **リテラルのスロット表現**（`ge` を `k` のまま →「k以上」、リテラル5 →「5以上」、L267）

## 順序マーカーが品質を決める

操作の適用順序は自由なので（L297）、**操作が2個以上の意味ASTでは適用順序を明示する接続表現を
必ず含める**（L304）。3個以上では順序マーカーを連鎖させ、どの操作が何番目かが一意に読み取れる
ようにする（L306）。

```
[even, add_k, desc, take_first]
  まず偶数だけを残し、次にそれぞれにkを加え、降順に並べてから先頭k個を取る
```

順序マーカーが無いと、`[asc, take_first]` と `[take_first, asc]` が同じ日本語指示になり、
**一つの指示に二つの正解が生じる。**

既定配分では **difficulty 4 が全体の58%** を占めるため、連鎖の品質がデータ全体の品質を決める。
L314「difficultyが3以上の指示文は特に注意して承認すること」を承認フローに反映すること。
"""

import random
from typing import Protocol

from boku.semantics.semantic_ast import Binding, SemanticAST


class InstructionRenderer(Protocol):
    """意味ASTから日本語の指示文を生成する。"""

    def render(self, ast: SemanticAST, binding: Binding, rng: random.Random) -> str:
        """指示文を1つ返す。

        Args:
            ast: 意味AST。`difficulty` が2以上なら順序マーカーが必須（改訂版 L304）。
            binding: `None` なら「k以上」のようにパラメータのまま、`int` なら「5以上」の
                ようにリテラルで書く（L267）。値は全スロット共通（実装計画 §2.4）。
            rng: 承認済み表現辞書から選ぶための乱数。**呼び出し側が種を管理する。**
                固定入力集合や hidden test とは別の名前空間を使うこと（実装計画 §3）。

        Returns:
            日本語の指示文。
        """
        ...
